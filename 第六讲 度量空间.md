### 线性函数与对偶空间

#### 定义：线性函数、对偶空间

设 $V$ 为 $F$-空间，$V$ 上的线性函数指线性映射 $f : V \to F$，记其构成集合 $V^* \triangleq \text{Hom}_F(V, F)$，称为 $V$ 的对偶空间。

[例 1] 取 $V = M_{m \times n}(F)$，任取 $B \in V$，定义：

$$
\begin{aligned}
f_B : V &\to F \\
A &\mapsto \text{tr}(B^T A)
\end{aligned}
$$

由迹的性质可知 $f_B \in V^*$。

[例 2] 取 $V = F[x]_n$，任取 $t \in F$，定义：

$$
\begin{aligned}
\sigma_t : V &\to F \\
f &\mapsto f(t)
\end{aligned}
$$

由代入定理可知 $\sigma_t \in V^*$。

#### 命题：线性函数的核与像

内容：设 $V$ 为 $F$-空间，$f \in V^* \backslash \{0\}$，则 $\text{Im } f = F$，且 $\dim \ker f = n - 1$。

#### 定义：对偶基

设 $V$ 为 $n$ 维 $F$-空间，取 $V$ 的一组基 $\alpha_1, \cdots, \alpha_n$，$\forall 1 \leq i \leq n$，定义：

$$
\begin{aligned}
\alpha_i^* : V &\to F \\
\alpha_j &\mapsto \delta_{i, j}, &\quad \forall 1 \leq j \leq n
\end{aligned}
$$

即 $\forall \alpha = \displaystyle\sum_{j = 1}^n k_j \alpha_j \in V, \alpha_i^*(\alpha) = k_i$。

则 $\alpha_1^*, \cdots, \alpha_n^*$ 为 $V^*$ 的一组基，称为其对偶基，且有 $\forall f \in V^*, f = \displaystyle\sum_{i = 1}^n f(\alpha_i) \alpha_i^*$。

#### 命题：原空间与对偶空间的基变换

内容：设 $V$ 为 $n$ 维 $F$-空间，$\alpha_1, \cdots, \alpha_n$ 与 $\beta_1, \cdots, \beta_n$ 分别为 $V$ 的两组基，若 $\begin{bmatrix} \beta_1 & \cdots & \beta_n \end{bmatrix} = \begin{bmatrix} \alpha_1 & \cdots & \alpha_n \end{bmatrix} C$，则 $\begin{bmatrix} \beta_1^* & \cdots & \beta_n^* \end{bmatrix} = \begin{bmatrix} \alpha_1^* & \cdots & \alpha_n^* \end{bmatrix} C^{-T}$。

证明：

- 设 $C^{-1} = (\tilde{c}_{i, j})_{1 \leq i, j \leq n}$，则 $\beta_i^*(\alpha_j) = \displaystyle\sum_{k = 1}^n \tilde{c}_{k, j} \beta_i^*(\beta_k) = \tilde{c}_{i, j}$。
- 故有 $\beta_i^* = \displaystyle\sum_{j = 1}^n \tilde{c}_{i, j} \alpha_j^*$，即 $\begin{bmatrix} \beta_1^* & \cdots & \beta_n^* \end{bmatrix} = \begin{bmatrix} \alpha_1^* & \cdots & \alpha_n^* \end{bmatrix} C^{-T}$。

#### 定理：对偶（在同构意义下）的自反性

内容：

- 设 $V$ 为有限维 $F$-空间，$\forall \alpha \in V$，定义：

$$
\begin{aligned}
\tilde{\alpha} : V^* &\to F \\
f &\mapsto f(\alpha)
\end{aligned}
$$

- 在此基础上，定义：

$$
\begin{aligned}
\tau : V &\to V^{**} \\
\alpha &\mapsto \tilde{\alpha}
\end{aligned}
$$

- 则 $\tau$ 为同构。

证明：

- $\tau$ 的线性性显然，又由 $\dim V^{**} = \dim V^* = \dim V$ 可知只需证明 $\tau$ 单。
- 若 $\alpha \in V$ 使 $\tau(\alpha) = \tilde{\alpha} = 0$，则 $\forall f \in V^*, \tilde{\alpha}(f) = f(\alpha) = 0$。
- 当 $\alpha \neq 0$，设 $V = \text{span}(\alpha) \oplus U$，取 $f$ 使得 $\forall k \in F, \beta \in U, f(k \alpha + \beta) = k$，则 $f(\alpha) = 1$，矛盾！
- 故 $\alpha = 0$，即 $\ker \tau = \{0\}$，故 $\tau$ 单。

上述同构说明：$V$ 可视同 $V^*$ 上的线性函数，故向量空间可以视为线性函数的集合。

这意味着在某种程度上，$V, V^*$ 是“平等”的，并引出了下面“形式平等”的定义。

#### 定义：配对 (pair)

定义二元运算如下：

$$
\begin{aligned}
(\cdot, \cdot) : V \times V^* &\to F \\
(\alpha, f) &\mapsto f(\alpha) = \tilde{\alpha}(f) \triangleq \alpha(f)
\end{aligned}
$$

- 最后的记号 $\alpha(f)$ 想表达的正是“将线性函数应用于向量上”。
- 上面的 $V \times V^* \to F$ 经由同构 $V \cong \text{Hom}_F(F, V)$ 和 $F \cong \text{Hom}_F(F, F)$，（反过来写）也可以视为线性映射的合成 $\text{Hom}(V, F) \times \text{Hom}(F, V) \to \text{Hom}(F, F)$。

易见配对运算关于其两个参数均是线性的。

#### 定义：零化子 (annihilator)

$\forall S \subset V$，定义 $S$ 的零化子：

$$
\text{Ann}(S) = \{f \in V^* \mid \forall s \in S, f(s) = 0\} \subset V^*
$$

由定义易见 $\text{Ann}(S)$ 为 $V^*$ 的子空间。

[例 3] 令 $V = (F^n)^*$，则 $f \in V$ 可以表示为 $f(x) = \begin{bmatrix} a_1 & \cdots & a_n \end{bmatrix} x$，取 $S = \{f_1, \cdots, f_m\} \subset V$，则 $\text{Ann}(S)$ 为线性方程组 $\begin{cases} f_1(x) = 0 \\ \cdots \\ f_m(x) = 0 \end{cases}$ 的解空间。

下面详细讨论零化子是怎样在原空间与对偶空间的**子空间**之间建立关联的。

#### 命题：零化子的性质

内容：

- 设 $V$ 为 $F$-空间，则：
- (1) 维数：当 $V$ 有限维，设 $U$ 为 $V$ 的子空间，则 $\dim \text{Ann}(U) = \dim V - \dim U$。
- (2) 反序性：设 $U_1 \subset U_2 \subset V$，则 $\text{Ann}(U_2) \subset \text{Ann}(U_1)$。
- (3) 对合性：当 $V$ 有限维，设 $U$ 为 $V$ 的子空间，若将 $V$ 与 $V^{**}$（通过前述同构 $\tau$）等同，则 $\text{Ann}(\text{Ann}(U)) = U$。
- (4) 当 $V$ 有限维，设 $U_1, U_2$ 为 $V$ 的子空间，则 $\text{Ann}(U_1 + U_2) = \text{Ann}(U_1) \cap \text{Ann}(U_2), \text{Ann}(U_1 \cap U_2) = \text{Ann}(U_1) + \text{Ann}(U_2)$。

证明：

- (1) 取 $U$ 的一组基 $\alpha_1, \cdots, \alpha_r$，扩充为 $V$ 的一组基 $\alpha_1, \cdots, \alpha_r, \beta_1, \cdots, \beta_{n - r}$。
- $\forall f = \displaystyle\sum_{i = 1}^r \lambda_i \alpha_i^* + \sum_{i = 1}^{n - r} \mu_i \beta_i^* \in V^*$，由 $\forall 1 \leq j \leq r, f(\alpha_j) = 0$ 可知 $\forall 1 \leq j \leq r, \lambda_j = 0$，则 $f \in \text{span}(\beta_1^*, \cdots, \beta_{n - r}^*)$，故 $\text{Ann}(U) \subset \text{span}(\beta_1^*, \cdots, \beta_{n - r}^*)$。
- 同时，显见 $\text{span}(\beta_1^*, \cdots, \beta_{n - r}^*) \subset \text{Ann}(U)$，故 $\text{Ann}(U) = \text{span}(\beta_1^*, \cdots, \beta_{n - r}^*)$。
- 因此 $\dim \text{Ann}(U) = n - r = \dim V - \dim U$。
- (2) 显然。
- (3) 同 (1) 可知 $\text{Ann}(U) = \text{span}(\beta_1^*, \cdots, \beta_{n - r}^*), \text{Ann}(\text{Ann}(U)) = \text{span}(\alpha_1^{**}, \cdots, \alpha_r^{**})$。
- 因此 $\tau(U) = \tau(\text{span}(\alpha_1, \cdots, \alpha_r)) = \text{span}(\alpha_1^{**}, \cdots, \alpha_r^{**}) = \text{Ann}(\text{Ann}(U))$，即在将 $V$ 与 $V^{**}$ 等同的意义下，$\text{Ann}(\text{Ann}(U)) = U$ 成立。
- (4) 取 $U_1 \cap U_2$ 的一组基，将其分别扩充为 $U_1, U_2$ 的一组基，再将前面得到的 $U_1 + U_2$ 的一组基扩充为 $V$ 的一组基，同 (1) 即得，细节略去。

~~事实上，向量空间 $V$ 的子空间集合 $\text{Sub}(V)$ 关于 $+, \cap$ 构成格。当 $V$ 有限维，(4) 指出 $\text{Ann}$ 为 $\text{Sub}(V) \to \text{Sub}(V^*)$ 的**格反同态**（即和与交运算取反），(3) 则进一步指出 $\text{Ann}$ 为**格反同构**。~~

#### 定义：对偶映射

设 $\mathcal{A} \in \text{Hom}(V_1, V_2)$，定义：

$$
\begin{aligned}
\mathcal{A}^* : V_2^* &\to V_1^* \\
f &\mapsto f \circ \mathcal{A}
\end{aligned}
$$

称为 $\mathcal{A}$ 的对偶映射。

由定义立即可得 $\mathcal{A}^* \in \text{Hom}(V_2^*, V_1^*)$，且有如下性质：

$$
\forall \alpha \in V_1, \beta \in V_1^*, (\alpha, \mathcal{A}^* \beta) = (\mathcal{A} \alpha, \beta)
$$

#### 命题：对偶映射的核与像的性质

内容：

- 设 $V_1, V_2$ 为有限维 $F$-空间，$\mathcal{A} \in \text{Hom}(V_1, V_2)$，则：
- (1) $\ker \mathcal{A}^* = \text{Ann}(\text{Im } \mathcal{A})$。
- (2) 若将 $V_1$ 与 $V_1^{**}$ 等同，$\text{Ann}(\text{Im } \mathcal{A}^*) = \ker \mathcal{A}$。
- (3) $\text{Im } \mathcal{A}^* = \text{Ann}(\ker \mathcal{A})$。
- (4) 若将 $V_2$ 与 $V_2^{**}$ 等同，$\text{Ann}(\ker \mathcal{A}^*) = \text{Im } \mathcal{A}$。

证明：

- (1)

$$
\begin{array}{l}
& f \in \ker \mathcal{A}^* \\
\Leftrightarrow & \mathcal{A}^* f = 0 \\
\Leftrightarrow & \forall \alpha \in V_1, (f \circ \mathcal{A}) \alpha = 0 \\
\Leftrightarrow & \forall \alpha \in V_1, f(\mathcal{A} \alpha) = 0 \\
\Leftrightarrow & \forall \beta \in \text{Im } \mathcal{A}, f(\beta) = 0 \\
\Leftrightarrow & f \in \text{Ann}(\text{Im } \mathcal{A})
\end{array}
$$

- 故有 $\ker \mathcal{A}^* = \text{Ann}(\text{Im } \mathcal{A})$。
- (2)

$$
\begin{array}{l}
& \alpha \in \ker \mathcal{A} \\
\Leftrightarrow & \mathcal{A} \alpha = 0 \\
\Leftrightarrow & \forall f \in V_2^*, f(\mathcal{A} \alpha) = 0 \\
\Leftrightarrow & \forall f \in V_2^*, (f \circ \mathcal{A}) \alpha = 0 \\
\Leftrightarrow & \forall f \in V_2^*, (\mathcal{A}^* f) \alpha = 0 \\
\Leftrightarrow & \forall g \in \text{Im } \mathcal{A}^*, g(\alpha) = 0 \\
\Leftrightarrow & \forall g \in \text{Im } \mathcal{A}^*, \tilde{\alpha}(g) = 0 \\
\Leftrightarrow & \tilde{\alpha} \in \text{Ann}(\text{Im } \mathcal{A}^*)
\end{array}
$$

- 故有 $\tau(\ker \mathcal{A}) = \text{Ann}(\text{Im } \mathcal{A}^*)$，将 $V_1$ 与 $V_1^{**}$ 等同后即得 $\text{Ann}(\text{Im } \mathcal{A}^*) = \ker \mathcal{A}$。
- (3) 一方面，$\forall \mathcal{A}^* \alpha^* \in \text{Im } \mathcal{A}^*$，$\forall \beta \in \ker \mathcal{A}, (\mathcal{A}^* \alpha^*) \beta = \alpha^*(\mathcal{A} \beta) = 0$，故 $\text{Im } \mathcal{A}^* \subset \text{Ann}(\ker \mathcal{A})$。
- 另一方面，$\forall f \in \text{Ann}(\ker \mathcal{A})$，设 $V_2 = \text{Im } \mathcal{A} \oplus U$，定义：

$$
\begin{aligned}
g : V_2 &\to F \\
\mathcal{A} \alpha + \beta &\mapsto \alpha, &\quad \forall \alpha \in V_1, \beta \in U
\end{aligned}
$$

- $g$ 的线性性与良定义性显然，同时由定义可知 $f = \mathcal{A}^* g$，故 $\text{Ann}(\ker \mathcal{A}) \subset \text{Im } \mathcal{A}^*$。
- 综上，$\text{Im } \mathcal{A}^* = \text{Ann}(\ker \mathcal{A})$。
- (4) 类似 (2) 做即可，略去。

#### 命题：矩阵视角下的对偶映射

内容：设 $V_1, V_2$ 分别为 $m, n$ 维 $F$-空间，$\mathcal{A} \in \text{Hom}(V_1, V_2)$，分别取 $V_1, V_2$ 的两组基 $\alpha_1, \cdots, \alpha_m$ 和 $\beta_1, \cdots, \beta_n$，设 $\mathcal{A} \begin{bmatrix} \alpha_1 & \cdots & \alpha_m \end{bmatrix} = \begin{bmatrix} \beta_1 & \cdots & \beta_n \end{bmatrix} A$，则 $\mathcal{A}^* \begin{bmatrix} \beta_1^* & \cdots & \beta_n^* \end{bmatrix} = \begin{bmatrix} \alpha_1^* & \cdots & \alpha_m^* \end{bmatrix} A^T$。

证明：设出 $A$ 的分量，按定义展开 $(\mathcal{A}^* \beta_i^*) \alpha_j$ 即可，略去。

### 双线性函数

#### 定义：双线性函数

设 $V_1, V_2$ 为 $F$-空间，$\varphi : V_1 \times V_2 \to F$，称 $\varphi$ 为 $V_1, V_2$ 上的双线性函数，若其关于每一个参数都是线性的。

记 $\text{Bil}_F(V_1, V_2)$ 为所有 $V_1, V_2$ 上的双线性函数构成的集合，易见其关于逐点加法和数乘构成 $F$-空间。

同样，在不致歧义的前提下，$\text{Bil}_F(V_1, V_2)$ 常略作 $\text{Bil}(V_1, V_2)$。

[例 4] 设 $f \in V_1^*, g \in V_2^*$，定义映射 $\varphi_{f, g}(\alpha, \beta) = f(\alpha) g(\beta)$，不难验证 $\varphi_{f, g}$ 也是一个双线性函数。

#### 双线性函数的矩阵表示

设 $V_1, V_2$ 有限维，分别取二者的两组基 $\alpha_1, \cdots, \alpha_n$ 和 $\beta_1, \cdots, \beta_m$。

设 $\alpha = \begin{bmatrix} \alpha_1 & \cdots & \alpha_n \end{bmatrix} x \in V_1, \beta = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} y \in V_2$，则：

$$
\varphi(\alpha, \beta) = \sum_{i = 1}^n \sum_{j = 1}^m x_i \cdot \varphi(\alpha_i, \beta_j) \cdot y_j
$$

记 $A = (\varphi(\alpha_i, \beta_j))_{i, j} \in M_{n \times m}(F)$，则有矩阵形式：

$$
\varphi(\alpha, \beta) = x^T Ay
$$

称 $A$ 为 $\varphi$ 在该基下的度量阵。易见选定基时，$\varphi$ 与 $A$ 一一对应，故有推论：

$$
\text{Bil}_F(V_1, V_2) \cong M_{n \times m}(F)
$$

同时易见 $\varphi(\alpha_i, \beta_j) = \varphi_{\alpha_i^*, \beta_j^*}(\alpha, \beta)$，故 $\{\varphi_{\alpha_i^*, \beta_j^*}\}$ 即为 $\text{Bil}(V_1, V_2)$ 的一组基。

#### 基变换下的双线性函数

设有基变换 $\begin{bmatrix} \alpha'_1 & \cdots & \alpha'_n \end{bmatrix} = \begin{bmatrix} \alpha_1 & \cdots & \alpha_n \end{bmatrix} P, \begin{bmatrix} \beta'_1 & \cdots & \beta'_m \end{bmatrix} = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} Q$，$\alpha, \beta$ 在新基下的坐标为 $x', y'$，则：

$$
\varphi(\alpha, \beta) = x^T Ay = (Px')^T A (Qy') = (x')^T (P^T AQ) y'
$$

因此 $\varphi$ 在新基下的度量阵为 $A' = P^T AQ$。

特别地，当 $V_1 = V_2 = V$ 且二者取同一组基，则 $A' = P^T AP$，即度量阵在基变换下合同。

#### 定义：正交子空间

设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，$U_1, U_2$ 分别为 $V_1, V_2$ 的子空间，定义 $U_1, U_2$ 关于 $\varphi$ 的正交子空间分别为：

$$
\begin{cases}
U_1^{\perp} = \{\beta \in V_2 \mid \forall \alpha \in U_1, \varphi(\alpha, \beta) = 0 \} \\
U_2^{\perp} = \{\alpha \in V_1 \mid \forall \beta \in U_2, \varphi(\alpha, \beta) = 0 \}
\end{cases}
$$

易见 $U_1^{\perp}, U_2^{\perp}$ 分别为 $V_2, V_1$ 的子空间。

特别地，当 $V_1 = V_2 = V$，记：

$$
\begin{cases}
U^{\perp_R} = \{\beta \in V \mid \forall \alpha \in U, \varphi(\alpha, \beta) = 0 \} \\
U^{\perp_L} = \{\alpha \in V \mid \forall \beta \in U, \varphi(\alpha, \beta) = 0 \}
\end{cases}
$$

其中 $V^{\perp_R}, V^{\perp_L}$ 分别称为 $V$ 的右根和左根。

#### 正交子空间的矩阵表示

设 $V_1, V_2$ 有限维，利用双线性函数的矩阵表示 $\varphi(\alpha, \beta) = x^T Ay$：

$$
\begin{aligned}
V_1^{\perp} &= \{\beta = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} y \in V_2 \mid \forall x \in F^n, x^T Ay = 0 \} \\
&= \{\beta = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} y \in V_2 \mid Ay = 0 \} \\
&= \{\beta = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} y \in V_2 \mid y \in \ker A \} \\
\end{aligned}
$$

同理有：

$$
V_2^{\perp} = \{\alpha = \begin{bmatrix} \alpha_1 & \cdots & \alpha_n \end{bmatrix} x \in V_1 \mid x \in \ker A^T \}
$$

可见 $\dim V_1^{\perp} = m - \text{rank}(A), \dim V_2^{\perp} = n - \text{rank}(A)$。

#### 定义：退化

设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$：

- 称 $\varphi$ 左退化，若 $V_2^{\perp} \neq \{0\}$；反正称其非左退化。
- 称 $\varphi$ 右退化，若 $V_1^{\perp} \neq \{0\}$；反正称其非右退化。
- 称 $\varphi$ 非退化，若其非左退化且非右退化。

#### 命题：退化的充要条件

内容：

- 设 $V_1, V_2$ 为有限维 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，则：
- (1) $\varphi$ 左退化当且仅当 $\varphi$ 在某组基下的度量阵列不满秩。
- (2) $\varphi$ 右退化当且仅当 $\varphi$ 在某组基下的度量阵行不满秩。
- (3) $\varphi$ 非退化当且仅当 $\dim V_1 = \dim V_2$ 且 $\varphi$ 在某组基下的度量阵可逆。

#### 定义：左、右伴随

设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，则 $\varphi$ 诱导出下面两个线性映射：

- (1) 左伴随

$$
\begin{aligned}
L_{\varphi} : V_1 &\to V_2^* \\
\alpha &\mapsto L_{\varphi}(\alpha) : \beta \mapsto \varphi(\alpha, \beta)
\end{aligned}
$$

- (2) 右伴随

$$
\begin{aligned}
R_{\varphi} : V_2 &\to V_1^* \\
\beta &\mapsto R_{\varphi}(\beta) : \alpha \mapsto \varphi(\alpha, \beta)
\end{aligned}
$$

~~你就说这是不是一种 partial application 吧！~~

#### 命题：左、右伴随的核与像

内容：

- 设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，则：
- (1) $\ker L_{\varphi} = V_2^{\perp}, \ker R_{\varphi} = V_1^{\perp}$。
- (2) 当 $V_1, V_2$ 有限维，设 $\varphi$ 在某组基下的度量阵为 $A$，则 $\text{rank}(L_{\varphi}) = \text{rank}(R_{\varphi}) = \text{rank}(A)$。

证明：

- (1) 由定义立即可得。
- (2) 设 $\varphi$ 在 $V_1$ 的基 $\alpha_1, \cdots, \alpha_n$ 和 $V_2$ 的基 $\beta_1, \cdots, \beta_m$ 下的度量阵为 $A = (a_{i, j})_{i, j}$。
- 考察 $L_{\varphi}$ 在 $V_1$ 的基 $\alpha_1, \cdots, \alpha_n$ 和 $V_2^*$ 的基 $\beta_1^*, \cdots, \beta_m^*$ 下的表现：

$$
(L_{\varphi}(\alpha_i))(\beta_j) = \varphi(\alpha_i, \beta_j) = a_{i, j}
$$

- 则 $L_{\varphi}$ 在这组基下的矩阵为 $A$，故 $\text{rank}(L_{\varphi}) = \text{rank}(A)$。
- 同理可知 $R_{\varphi}$ 在 $V_2$ 的基 $\beta_1, \cdots, \beta_m$ 和 $V_1^*$ 的基 $\alpha_1^*, \cdots, \alpha_n^*$ 下的矩阵为 $A^T$，故 $\text{rank}(L_{\varphi}) = \text{rank}(A^T) = \text{rank}(A)$。

#### 推论：退化与左、右伴随的关系

内容：

- 设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，则：
- (1) $\varphi$ 非左退化 $\Leftrightarrow L_{\varphi}$ 单，$\varphi$ 非右退化 $\Leftrightarrow R_{\varphi}$ 单。
- (2) 当 $V_1, V_2$ 有限维，$\varphi$ 非退化 $\Leftrightarrow L_{\varphi}$ 为同构 $\Leftrightarrow R_{\varphi}$ 为同构。
- _此时可以视 $V_1, V_2$ 为彼此的对偶空间。_

注意到 (2) 要求**有限维**，事实上无限维的情况可以构造反例如下：

- 令 $V_1 = V_2 = \mathbb{R}[x]$，定义 $\varphi \in \text{Bil}(\mathbb{R}[x], \mathbb{R}[x])$ 满足 $\varphi(x^i, x^j) = \delta_{i, j}$，则：

$$
\forall f(x) = \sum_{i = 0}^n a_i x^i, g(x) = \sum_{i = 0}^n b_i x^i \in \mathbb{R}[x], \varphi(f, g) = \sum_{i = 0}^n a_i b_i
$$

- (I) 若 $f(x) = \displaystyle\sum_{i = 0}^n a_i x^i \in \mathbb{R}[x]$ 使得 $L_{\varphi}(f) = 0$，则 $(L_{\varphi}(f))(f) = \varphi(f, f) = \displaystyle\sum_{i = 0}^n a_i^2 = 0$，故 $a_0 = \cdots = a_n = 0$，即 $f = 0$。
- 故 $L_{\varphi}$ 单，则 $\varphi$ 非左退化。同理可知 $\varphi$ 非右退化，故 $\varphi$ 非退化。
- (II) 定义 $T \in \mathbb{R}[x]^*$ 满足 $T(x^i) = 1$，则：

$$
\forall f(x) = \sum_{i = 0}^n a_i x^i \in \mathbb{R}[x], T(f) = \sum_{i = 0}^n a_i
$$

- 若 $\tilde{T} \in \mathbb{R}[x]$ 使得 $L_{\varphi}(\tilde{T}) = T$，则 $\forall i \in \mathbb{N}$，应用 $x^i$ 可见 $\tilde{T}$ 的 $x^i$ 项为 $1$，与 $\tilde{T}$ 系数组只有有限个 $0$ 矛盾。
- 故 $L_{\varphi}$ 不为同构。同理可知 $R_{\varphi}$ 也不为同构。

#### Riesz 表示定理（有限维 ver.）

内容：设 $V_1, V_2$ 为有限维 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$ 非退化，则：

$$
\begin{cases}
\forall f \in V_1^*, \exists ! \beta \in V_2, \text{s.t. } f = R_{\varphi}(\beta) \\
\forall g \in V_2^*, \exists ! \alpha \in V_1, \text{s.t. } g = L_{\varphi}(\alpha)
\end{cases}
$$

证明：由上述推论 (2) 立即可得。

#### 定义 & 定理：伴随映射

设 $V_1, V_2$ 为 $F$-空间，$\varphi_1 \in \text{Bil}(V_1, V_1), \varphi_2 \in \text{Bil}(V_2, V_2)$ 非退化，给定 $\mathcal{A} \in \text{Hom}(V_1, V_2)$，称 $\mathcal{A}^* \in \text{Hom}(V_2, V_1)$ 为其伴随映射，若：

$$
\forall \alpha \in V_1, \beta \in V_2, \varphi_1(\alpha, \mathcal{A}^* \beta) = \varphi_2(\mathcal{A} \alpha, \beta) \quad (*)
$$

当 $V_1, V_2$ **有限维**，$\forall \beta \in V_2$，定义线性映射：

$$
\begin{aligned}
f_{\beta} : V_1 &\to F \\
\alpha &\mapsto \varphi_2(\mathcal{A} \alpha, \beta)
\end{aligned}
$$

由 $\varphi_1$ 非退化且 $f_{\beta} \in V_1^*$，据 Riesz 表示定理可知：

$$
\exists ! \alpha_0 \in V_1, \text{s.t. } f_{\beta} = L_{\varphi_1}(\alpha_0)
$$

令 $\mathcal{A}^* \beta = \alpha_0$，则满足 $(*)$ 的 $\mathcal{A}^* : V_2 \to V_1$ **存在且唯一**。下面验证其保线性：

- (1) 保持加法：$\forall \beta_1, \beta_2 \in V_2$，$\forall \alpha \in V_1$，有：

$$
\begin{array}{l}
& \varphi_1(\alpha, \mathcal{A}^*(\beta_1 + \beta_2)) \\
= & \varphi_2(\mathcal{A}(\alpha), \beta_1 + \beta_2) \\
= & \varphi_2(\mathcal{A}(\alpha), \beta_1) + \varphi_2(\mathcal{A}(\alpha), \beta_2) \\
= & \varphi_1(\alpha, \mathcal{A}^*(\beta_1)) + \varphi_1(\alpha, \mathcal{A}^*(\beta_2)) \\
= & \varphi_1(\alpha, \mathcal{A}^*(\beta_1) + \mathcal{A}^*(\beta_2))
\end{array}
$$

- 故 $R_{\varphi_1}(\mathcal{A}^*(\beta_1 + \beta_2)) = R_{\varphi_1}(\mathcal{A}^*(\beta_1) + \mathcal{A}^*(\beta_2))$，由 $\varphi_1$ 非退化可知 $R_{\varphi_1}$ 单，遂有：

$$
\mathcal{A}^*(\beta_1 + \beta_2) = \mathcal{A}^*(\beta_1) + \mathcal{A}^*(\beta_2)
$$

- (2) 保持数乘：同 (1) 可证，略去。

综上，$\mathcal{A}$ 的伴随映射 $\mathcal{A}^*$ **存在且唯一**。

------

上面讨论的 $\mathcal{A}$ 作用于第一个参数的伴随映射称为左伴随，记作 $\mathcal{A}^{L*}$。同理还可以定义 $\mathcal{A}$ 作用于第二个参数的右伴随 $\mathcal{A}^{R*} \in \text{Hom}(V_2, V_1)$：

$$
\forall \alpha \in V_1, \beta \in V_2, \varphi_1(\mathcal{A}^{R*} \beta, \alpha) = \varphi_2(\beta, \mathcal{A} \alpha)
$$

### 度量与度量空间

#### 定义：度量空间

设 $V$ 为 $F$-空间，$\varphi \in \text{Bil}(V, V)$，则称二元组 $(V, \varphi)$ 为度量空间，其中 $\varphi$ 称为 $V$ 的度量。

特别地，当 $\varphi$ 非退化，称 $(V, \varphi)$ 为非退化 / 正则度量空间。

#### 定义：等距同构 (isometry)

设 $(V_1, \varphi_1)$ 与 $(V_2, \varphi_2)$ 为域 $F$ 上的度量空间，称（线性）同构 $\sigma \in \text{Hom}(V_1, V_2)$ 为从 $(V_1, \varphi_1)$ 到 $(V_2, \varphi_2)$ 的等距同构，若其保持度量，即：

$$
\forall \alpha, \beta \in V_1, \varphi_1(\alpha, \beta) = \varphi_2(\sigma \alpha, \sigma \beta)
$$

此时也称 $(V_1, \varphi_1), (V_2, \varphi_2)$ 等距。记 $\Sigma_F = \{\text{域 } F \text{ 上的有限维度量空间}\}$，易见等距是 $\Sigma_F$ 上的一个**等价关系**。

特别地，当 $V_1 = V_2 = V$ 且 $\varphi_1 = \varphi_2 = \varphi$，所有等距同构构成的集合关于映射的复合成为一个**群**，记作 $\text{Isom}(V, \varphi)$，称为 $(V, \varphi)$ 的等距变换群。

[例 5] 设 $(V, \varphi)$ 为域 $F$ 上的 $n$ 维度量空间，任取 $V$ 的一组基诱导出坐标同构 $\sigma_V \in \text{Hom}(V, F^n)$，设 $\varphi$ 在这组基下的度量阵为 $A$，定义 $F^n$ 上的双线性型：

$$
\begin{aligned}
\tilde{\varphi} : F^n \times F^n &\to F \\
(x, y) &\mapsto x^T Ay
\end{aligned}
$$

则 $\sigma_V$ 为从 $(V, \varphi)$ 到 $(F^n, \tilde{\varphi})$ 的等距同构。

#### 等距同构的矩阵表示

设 $(V_1, \varphi_1)$ 与 $(V_2, \varphi_2)$ 为域 $F$ 上的 $n$ 维度量空间，分别取 $V_1, V_2$ 的两组基 $\alpha_1, \cdots, \alpha_n$ 和 $\beta_1, \cdots, \beta_n$，设 $\varphi_1, \varphi_2$ 在这两组基下的度量阵分别为 $G_1, G_2$。

设 $\sigma \in \text{Hom}(V_1, V_2)$ 在这两组基下的变换阵为 $A$，则 $\sigma$ 为等距同构当且仅当：

$$
\forall x, y \in F^n, x^T G_1 y = (Ax)^T G_2 (Ay) = x^T (A^T G_2 A) Y
$$

由 $x, y$ 的任意性即得 $G_1 = A^T G_2 A$。又由于同一双线性函数在不同基下的度量阵合同，故有：

- 度量空间等距同构当且仅当二者的度量在各自空间的一组基下的矩阵**合同**。

因此，度量空间的分类问题转化为合同矩阵的分类问题。

#### 定义：正交

设 $(V, \varphi)$ 为域 $F$ 上的度量空间，对于 $\alpha, \beta \in V$，称 $\alpha$ 正交于 $\beta$，若 $\varphi(\alpha, \beta) = 0$，记作 $\alpha \perp \beta$。

在一般的度量空间中，$\alpha \perp \beta$ 不能推出 $\beta \perp \alpha$：因为没有给定 $\varphi$ 的**对称性**。下面先解释两类具有特殊对称性的度量空间。

#### 定义：对称与反对称、正交空间 (orthogonal space) 与辛空间 (symplectic space)

设 $(V, \varphi)$ 为域 $F$ 上的度量空间，称：

- (1) $\varphi$ 对称，若 $\forall \alpha, \beta \in V, \varphi(\alpha, \beta) = \varphi(\beta, \alpha)$。
- (2) $\varphi$ 反对称，若 $\forall \alpha, \beta \in V, \varphi(\alpha, \beta) = -\varphi(\beta, \alpha)$。

称该度量空间为正交空间 / 辛空间，若 $\varphi$ 对称 / 反对称。

由定义容易推知以下基本性质：

- (1) $\varphi$ 对称 / 反对称 $\Leftrightarrow$ $\varphi$ 在 $V$ 的一组基下的度量阵对称 / 反对称。
- (2) 当 $\text{char } F \neq 2$，$\varphi$ 反对称 $\Leftrightarrow \forall \alpha \in V, \varphi(\alpha, \alpha) = 0$。

下面来研究正交关系何时具有对称性。

#### Artin 定理

内容：设 $(V, \varphi)$ 为域 $F$ 上的度量空间，则其上的正交关系具有对称性当且仅当 $\varphi$ 对称或反对称。

证明：

- 充分性显然，下证必要性。
- (1) 若 $\forall \eta \in V, \varphi(\eta, \eta) = 0$，则 $\varphi$ 反对称。
- (2) 否则，$\exists \eta_0 \in V, \text{s.t. } \varphi(\eta_0, \eta_0) \neq 0$。
- 引理：$\forall \alpha, \beta, \gamma \in V, \varphi(\alpha, \beta) \varphi(\gamma, \alpha) = \varphi(\beta, \alpha) \varphi(\alpha, \gamma)$。
- 证明：令 $\delta = \varphi(\alpha, \beta) \gamma - \varphi(\alpha, \gamma) \beta$，则 $\alpha \perp \delta$，故 $\delta \perp \alpha$，整理即得目标形式。
- (I) 取 $\alpha = \beta = \eta_0$，可得 $\forall \gamma \in V, \varphi(\eta_0, \eta_0) \varphi(\gamma, \eta_0) = \varphi(\eta_0, \eta_0) \varphi(\eta_0, \gamma) \Rightarrow \varphi(\gamma, \eta_0) = \varphi(\eta_0, \gamma)$。
- (II) 取 $\gamma = \eta_0$，可得 $\forall \alpha, \beta \in V, \varphi(\alpha, \beta) \varphi(\eta_0, \alpha) = \varphi(\beta, \alpha) \varphi(\alpha, \eta_0)$。
- (III) 若 $\varphi(\eta_0, \alpha) = \varphi(\alpha, \eta_0) \neq 0$，则 $\varphi(\alpha, \beta) = \varphi(\beta, \alpha)$。
- 否则，有 $\varphi(\eta_0, \alpha + \eta_0) = \varphi(\alpha + \eta_0, \eta_0) = \varphi(\eta_0, \eta_0) \neq 0$，进而有 $\varphi(\alpha + \eta_0, \beta) = \varphi(\beta, \alpha + \eta_0)$，消去等式两边的 $\varphi(\eta_0, \beta) = \varphi(\beta, \eta_0)$ 即可得证。
- 综上，$\varphi$ 对称。

------

下面默认我们所研究的度量空间上的正交关系都满足对称性，即要么是正交空间、要么是辛空间。

此时 $V$ 的左根与右根一致，统一记作 $V^{\perp}$，称为 $V$ 的根。

#### 定义：根空间 (radical)

设 $(V, \varphi)$ 为域 $F$ 上的度量空间，$U$ 为 $V$ 的子空间，定义 $U$ 的根空间为：

$$
\text{Rad}(U) \triangleq \{\beta \in U \mid \forall \alpha \in U, \varphi(\alpha, \beta) = 0\} = U^{\perp} \cap U
$$

特别地，有 $\text{Rad}(V) = V^{\perp}$。

[例 5] 设 $F$ 为域，在 $F^4$ 中，定义 $\varphi(x, y) = x_1 y_1 + x_2 y_2$，则 $\varphi$ 对称，且其在标准正交基 $e_1, e_2, e_3, e_4$ 下的矩阵为：

$$
\begin{bmatrix}
1 \\
& 1 \\
& & 0 \\
& & & 0
\end{bmatrix}
$$

则 $U \triangleq \text{Rad}(F^4, \varphi) = \text{span}(e_3, e_4) \subset V$，故 $\varphi|_U = 0$，此时有 $U^{\perp} = F^4$。

#### 定义：迷向向量、迷向空间、全迷向空间

设 $(V, \varphi)$ 为域 $F$ 上的度量空间：

- (1) 称 $\alpha \in V \backslash \{0\}$ 为迷向向量，若 $\varphi(\alpha, \alpha) = 0$。
- (2) 称 $(V, \varphi)$ 为迷向空间，若 $V$ 中包含迷向向量。
- (3) 称 $(V, \varphi)$ 为全迷向空间，若 $\varphi = 0$。

例如：例 5 中的 $(U, \varphi|_U)$ 是全迷向空间。

#### 定理：非退化空间上的正交补空间的性质

设 $(V, \varphi)$ 为域 $F$ 上的非退化有限维度量空间，$U$ 为 $V$ 的子空间，则：

- (1) 正交补空间的维数公式：$\dim V = \dim U + \dim U^{\perp}$。
- (2) 正交补的对合性：$(U^{\perp})^{\perp} = U$。

证明：

- (1) 构造右伴随限制映射：

$$
\begin{aligned}
\tau : V &\to U^* \\
\beta &\mapsto R_{\varphi}(\beta)|_U
\end{aligned}
$$

- (I) $\ker \tau = \{\beta \in V \mid R_{\varphi}(\beta)|_U = 0\} = \{\beta \in V \mid \forall \alpha \in U, \varphi(\alpha, \beta) = 0\} = U^{\perp}$。
- (II) 设 $V = U \oplus W$，$\forall f \in U^*$，将其延拓为 $V$ 上的线性函数：

$$
\begin{aligned}
\tilde{f} : V &\to F \\
\alpha + \beta &\mapsto f(\alpha), &\quad \alpha \in U, \beta \in W
\end{aligned}
$$

- 由 $(V, \varphi)$ 非退化，据 Riesz 表示定理可知 $\exists ! \beta \in V, \text{s.t. } R_{\varphi}(\beta) = \tilde{f}$，故 $\tau(\beta) = R_{\varphi}(\beta)|_U = \tilde{f}|_U = f$。
- 因此 $\tau$ 为满射，即 $\text{Im } \tau = U^*$。
- 综上 (I)(II)，由线性映射的维数公式可知 $\dim V = \dim \text{Im } \tau + \dim \ker \tau = \dim U + \dim U^{\perp}$。
- (2) $\forall \alpha \in U, \beta \in U^{\perp}, \varphi(\alpha, \beta) = 0$，故 $\alpha \in (U^{\perp})^{\perp}$，由 $\alpha$ 的任意性即得 $U \subset (U^{\perp})^{\perp}$。
- 同时，由 (1) 可知 $\dim V = \dim U + \dim U^{\perp} = \dim U^{\perp} + \dim (U^{\perp})^{\perp}$，故有 $\dim U = \dim (U^{\perp})^{\perp}$。
- 综合包含关系与维数关系即得 $(U^{\perp})^{\perp} = U$。

自然地，正交子空间的维数公式让我们猜想 $V = U \oplus U^{\perp}$，下面来对这种“正交且直和”的结构进行进一步的讨论。

#### 定义：正交直和

设 $(V, \varphi)$ 为域 $F$ 上的度量空间，$S, T$ 为 $V$ 的子空间，称 $V$ 为 $S, T$ 的正交直和，若：

- (1) 直和：$V = S \oplus T$。
- (2) 正交：$S \perp T$，即 $\forall \alpha \in S, \beta \in T, \alpha \perp \beta$。

记作 $V = S \oplus^{\perp} T$。

#### 命题：子空间非退化的等价条件

内容：设 $(V, \varphi)$ 为域 $F$ 上的非退化度量空间，$U$ 为 $V$ 的子空间，则下列命题等价：

- (1) $U$ 非退化。
- (2) $U^{\perp}$ 非退化。
- (3) $V = U \oplus U^{\perp}$。
- (4) $V = U \oplus^{\perp} U^{\perp}$。

证明：

- $(1) \Leftrightarrow (2)$：$U$ 非退化 $\Leftrightarrow \text{Rad}(U) = \{0\} \Leftrightarrow U^{\perp} \cap U = \{0\} \Leftrightarrow (U^{\perp})^{\perp} \cap U^{\perp} = \{0\} \Leftrightarrow \text{Rad}(U^{\perp}) = \{0\} \Leftrightarrow U^{\perp}$ 非退化。
- $(1) \Rightarrow (3)$：$U \cap U^{\perp} = \text{Rad}(U) = \{0\}$，又由正交补空间的维数公式可知 $\dim(U + U^{\perp}) = \dim V$，故 $V = U \oplus U^{\perp}$。
- $(3) \Leftrightarrow (4)$：由定义即得。

#### 命题：退化度量空间的正交直和分解

内容：设 $(V, \varphi)$ 为域 $F$ 上的度量空间，$W$ 为 $\text{Rad}(V)$ 的补空间，则 $V = \text{Rad}(V) \oplus^{\perp} W$，且 $W$ 非退化。

证明：

- 正交直和显见，只需证明 $W$ 非退化。
- 若 $\alpha \in \text{Rad}(W)$，则 $\alpha \perp W, \alpha \perp \text{Rad}(V) \Rightarrow \alpha \perp V$，故 $\alpha \in \text{Rad}(V)$。
- 由 $\text{Rad}(W) \subset W$ 可知 $\alpha \subset \text{Rad}(V) \cap W = \{0\}$，故 $\alpha = 0$。
- 因此 $\text{Rad}(W) = \{0\}$，即 $W$ 非退化。

鉴于退化度量空间总能通过正交直和分解将“退化部分”分离出去，下面我们来研究具体的**非退化**度量空间的结构。

------

首先来研究非退化辛空间的结构。

#### 定理：非退化辛空间的结构

内容：

- 设 $(V, \varphi)$ 为有限维非退化（非零）辛空间，则 $\dim V$ 为偶数。
- 设 $\dim V = 2s$，则 $V$ 可以分解为 $s$ 个二维辛空间的正交直和：

$$
V = \left. \bigoplus_{i = 1}^s \right.^{\perp} H_i
$$

- 且存在一组基（称为**辛基**），使其在这组基下的矩阵 $G$ 为块对角阵：

$$
G = \text{diag} \left( \begin{bmatrix} 0 & 1 \\ -1 & 0 \end{bmatrix}, \cdots, \begin{bmatrix} 0 & 1 \\ -1 & 0 \end{bmatrix} \right)
$$

证明：

- (1) 当 $\dim V = 1$，取唯一的基向量 $\alpha \in V$，有 $\varphi(\alpha, \alpha) = 0$，故 $\varphi = 0$，与 $V$ 非退化矛盾！
- (2) 当 $\dim V \geq 2$，由 $V$ 非退化可知 $\exists \alpha, \beta \in V, \text{s.t. } \varphi(\alpha, \beta) \neq 0$。
- 显见 $\alpha, \beta$ 线性无关。令 $\beta \leftarrow \dfrac{\beta}{\varphi(\alpha, \beta)}$，则 $\varphi(\alpha, \beta) = 1$，进而有 $\varphi(\beta, \alpha) = -1$。
- 令 $H_1 = \text{span}(\alpha, \beta)$，则 $H_1$ 在基 $\alpha, \beta$ 下的度量阵为 $\begin{bmatrix} 0 & 1 \\ -1 & 0 \end{bmatrix}$，则 $H_1$ 非退化。
- (I) 当 $V = H_1$，$\alpha, \beta$ 即为 $V$ 的一组辛基。
- (II) 否则由子空间非退化的等价条件可知 $V = H_1 \oplus^{\perp} H_1^{\perp}$，其中 $H_1^{\perp}$ 也为非退化辛空间，且 $\dim H_1^{\perp} = \dim V - 2$。
- 对维数归纳，组合 $\alpha, \beta$ 和 $H_1^{\perp}$  的辛基即得 $V$ 的一组辛基。

由有限维非退化辛空间与同维数**非奇异**反对称阵间的同构关系可知：

- 非奇异反对称阵必为偶数阶，且与如下块对角阵合同：

$$
\text{diag} \left( \begin{bmatrix} 0 & 1 \\ -1 & 0 \end{bmatrix}, \cdots, \begin{bmatrix} 0 & 1 \\ -1 & 0 \end{bmatrix} \right)
$$

方便起见，设 $\varphi|_{H_i}$ 在基 $e_i, e_{-i}$ 下的度量阵为 $\begin{bmatrix} 0 & 1 \\ -1 & 0 \end{bmatrix}$，我们常将辛基排列为：

$$
e_1, \cdots, e_s, e_{-1}, \cdots, e_{-s}
$$

则 $\varphi$ 在这样排列的辛基下的度量阵为块副对角阵：

$$
J_s \triangleq \begin{bmatrix} O_s & I_s \\ -I_s & O_s \end{bmatrix}
$$

这意味着 $V$ 可以分解为两个**全迷向子空间**的直和：

$$
V = \text{span}(e_1, \cdots, e_s) \oplus \text{span}(e_{-1}, \cdots, e_{-s})
$$

#### 向量在辛基下的坐标表示

设 $\alpha = \displaystyle\sum_{i = 1}^s k_i e_i + \sum_{i = 1}^s l_i e_{-i} \in V$，则可通过 $\varphi$ 与辛基向量提取坐标：

$$
\varphi(\alpha, e_i) = -l_i, \varphi(\alpha, e_{-i}) = k_i, \quad 1 \leq j \leq s
$$

故有类似于“借由内积投影到标准正交基上”的展开式：

$$
\alpha = \sum_{i = 1}^s \varphi(\alpha, e_{-i}) e_i - \sum_{i = 1}^s \varphi(\alpha, e_i) e_{-i}
$$

#### 定义：辛变换、辛矩阵、辛群

设 $(V, \varphi)$ 为域 $F$ 上的辛空间，称同构 $\mathcal{A} \in \text{End}(V)$ 为辛变换，若其为等距同构。

取 $V$ 为一组辛基，设 $\mathcal{A}$ 在辛基下的矩阵为 $A$，则 $\mathcal{A}$ 为辛变换当且仅当：

$$
A^T J_s A = J_s
$$

称满足此条件的 $2s$ 阶可逆矩阵 $A$ 为辛矩阵。

易见 $F$ 上的所有 $2s$ 阶辛矩阵关于矩阵乘法构成群，记为 $\text{Sp}(2s, F)$，称为辛群。

#### 命题：辛矩阵的分块条件

内容：将 $A \in M_{2s}(F)$ 写成 $s \times s$ 的分块矩阵 $A = \begin{bmatrix} A_1 & A_2 \\ A_3 & A_4 \end{bmatrix}$，则 $A \in \text{Sp}(2s, F)$ 当且仅当：

$$
\begin{cases}
A_1^T A_3 = A_3^T A_1 &\quad (\text{即 } A_1^T A_3 \text{ 为对称阵}) \\
A_2^T A_4 = A_4^T A_2 &\quad (\text{即 } A_2^T A_4 \text{ 为对称阵}) \\
A_1^T A_4 - A_3^T A_2 = I_s
\end{cases}
$$

------

在下面对双曲平面和双曲对的讨论中，统一要求域 $F$ 满足 $\text{char}(F) \neq 2$。

#### 定义：双曲平面、双曲对

称域 $F$ 上的二维度量空间 $(V, \varphi)$ 为双曲平面，若其非退化且为迷向空间。

设 $(V, \varphi)$ 为域 $F$ 上的度量空间，称 $\alpha, \beta \in V$ 为双曲对，若：

$$
\begin{cases}
\varphi(\alpha, \alpha) = \varphi(\beta, \beta) = 0 \\
\varphi(\alpha, \beta) = 1
\end{cases}
$$

此时对正交空间有 $\varphi(\beta, \alpha) = 1$，对辛空间有 $\varphi(\beta, \alpha) = -1$。

非退化辛空间的正交直和分解中的每一个二维子空间都是一个双曲平面，每个子空间的辛基都是一个双曲对。

#### 命题：双曲平面由双曲对张成

内容：设二维度量空间 $(V, \varphi)$ 为双曲平面，则 $\exists$ 双曲对 $\alpha, \beta \in V$，$\text{s.t. } V = \text{span}(\alpha, \beta)$。

证明：

- 由 $(V, \varphi)$ 为迷向空间可知 $\exists \alpha \in V \backslash \{0\}, \text{s.t. } \varphi(\alpha, \alpha) = 0$。
- 由 $(V, \varphi)$ 非退化可知 $\exists \gamma \in V, \text{s.t. } \varphi(\alpha, \gamma) \neq 0$。
- (1) 若 $V$ 为正交空间，令 $\beta = \dfrac{1}{\varphi(\alpha, \gamma)} \gamma - \dfrac{\varphi(\gamma, \gamma)}{2 \varphi(\alpha, \gamma)^2} \alpha$。
- (2) 若 $V$ 为辛空间，令 $\beta = \dfrac{\gamma}{\varphi(\alpha, \gamma)}$。
- 总之此时有 $\varphi(\beta, \beta) = 0, \varphi(\alpha, \beta) = 1$，故其为双曲对。
- 显见 $\alpha, \beta$ 线性无关，故 $V = \text{span}(\alpha, \beta)$。

在正交的双曲平面 $V$ 中，$\varphi$ 在双曲对 $\alpha, \beta$ 下的度量阵为：

$$
\begin{bmatrix}
0 & 1 \\
1 & 0
\end{bmatrix}
$$

令 $\alpha' = \alpha + \dfrac{1}{2} \beta, \beta' = \alpha - \dfrac{1}{2} \beta$，则 $\alpha', \beta'$ 也为 $V$ 的一组基，且 $\varphi$ 在这组基下的矩阵为：

$$
\begin{bmatrix}
1 & 0 \\
0 & -1
\end{bmatrix}
$$

这恰好对应于解析几何中的双曲线 $x^2 - y^2 = c$！此即“双曲对”一词的几何渊源。

------

接下来研究非退化正交空间的结构。首先给出二次型的定义。

#### 定义 I（齐次多项式 pov.）：$r$ 次型

设 $f(x_1, \cdots, x_n) \in F[x_1, \cdots, x_n]$ 为 $r$ 次齐次多项式，取 $n$ 维 $F$-空间 $V$ 的一组基 $\alpha_1, \cdots, \alpha_n$ 后定义映射：

$$
\begin{aligned}
f : V &\to F \\
\alpha &\mapsto f(x_1, \cdots, x_n), &\quad \alpha = \sum_{i = 1}^n x_i \alpha_i
\end{aligned}
$$

称这样得到的 $f$ 为 $r$ 次型。特别地：

- 当 $r = 1$，$f$ 为线性函数。
- 当 $r = 2$，$f$ 为**二次型**。

#### 定义 II（双线性函数 pov.）：二次型

设域 $F$ 满足 $\text{char}(F) \neq 2$，$V$ 为 $F$-空间，称 $Q : V \to F$ 为二次型，若：

- (1) $\forall k \in F, \alpha \in V, Q(k \alpha) = k^2 Q(\alpha)$。
- (2) $q(\alpha, \beta) = \dfrac{1}{2}(Q(\alpha + \beta) - Q(\alpha) - Q(\beta))$ 诱导出双线性函数 $q \in \text{Bil}(V, V)$。

由定义可见，一旦 $Q$ 为二次型，诱导出的 $q$ 就是**对称**双线性函数，故 $(V, q)$ 为**正交空间**。

显见二次型 $Q$ 与对称双线性函数 $q$ 通过 $Q(\alpha) = q(\alpha, \alpha)$ **一一对应**，故研究二次型空间 $(V, Q)$ 与研究正交空间 $(V, Q)$ 完全等价。

#### 定理：非退化正交空间的结构

内容：

- 设 $(V, \varphi)$ 为 $n$ 维非退化正交空间，则 $V$ 可以分解为 $n$ 个一维子空间的正交直和：

$$
V = \left. \bigoplus_{i = 1}^n \right.^{\perp} \text{span}(\alpha_i)
$$

- 其中 $\alpha_1, \cdots, \alpha_n$ 满足：

$$
[\varphi(\alpha_i, \alpha_j) \neq 0] = \delta_{i, j}, \quad \forall 1 \leq i, j \leq n
$$

- 称为 $V$ 的一组**正交基**。在这组基下，$\varphi$ 的度量阵 $G$ 为对角阵：

$$
G = \text{diag}(\varphi(\alpha_1, \alpha_1), \cdots, \varphi(\alpha_n, \alpha_n))
$$

证明：

- 由 $(V, \varphi)$ 非退化可知 $\exists \alpha_1, \text{s.t. } \varphi(\alpha_1, \alpha_1) \neq 0$。
- (1) 若 $n = 1$，分解完成。
- (2) 若 $n > 1$，令 $U = \text{span}(\alpha_1)$，则 $(U, \varphi|_U)$ 非退化，故有正交直和分解 $V = U \oplus^{\perp} U^{\perp}$。
- 此时 $(U^{\perp}, \varphi|_{U^{\perp}})$ 亦为非退化正交空间，且有 $\dim U^{\perp} = n - 1$。
- 综上 (1)(2)，对 $n$ 归纳即可。

类似于辛空间的情形，由有限维非退化正交空间与同维数**非奇异**对称阵间的同构关系可知：

- 非奇异对称阵与一个**对角元素非零**的对角阵合同。

推及允许奇异阵的情形，可见对称阵必与一个对角阵合同：在右下角补零即可。

我们自然期待能将“对角阵”化为更简单的形式——比如只有 $1, 0$ 这样的“最简形式”。但事实上这并不一定能做到，而是依赖于域 $F$ 的性质。下面进行进一步的讨论。

#### 一维正交空间的等距同构分类

设 $(V, \varphi)$ 为一维正交空间，$V = \text{span}(\alpha)$。

当 $\varphi(\alpha, \alpha) = 0$ 则退化，其构成一个单独的等价类。

否则非退化，设 $\varphi(\alpha, \alpha) = a \in F^*$，此时 $V$ 上的同构均为纯量变换 $\mathcal{K} : \alpha \mapsto c \alpha \ (c \in F^*)$。

若 $\mathcal{K}$ 为从 $(V, \varphi)$ 到 $(V, \varphi')$ 的等距同构，则有：

$$
a = \varphi(\alpha, \alpha) = \varphi'(\mathcal{K} \alpha, \mathcal{K} \alpha) = \varphi'(c \alpha, c \alpha) = c^2 \varphi'(\alpha, \alpha)
$$

即 $(\alpha, \alpha)$ 的度量值在同构下相差一个**非零平方因子**。

又鉴于 $\varphi$ 与 $\varphi(\alpha, \alpha) = a$ **一一对应**，一维非退化正交空间的等距同构完全由商群 $F^* / F^{*2}$（即域的乘法群模掉所有非零平方因子构成的子群）决定。

特别地：

- 当 $F = \mathbb{C}$，可取商群 $\mathbb{C}^* / \mathbb{C}^{*2}$ 的代表元为 $1$。
- 当 $F = \mathbb{R}$，可取商群 $\mathbb{R}^* / \mathbb{R}^{*2}$ 的代表元为 $\pm 1$。

现在来研究 $F = \mathbb{R}$ 即实正交空间的情形。

#### 定义：符号 (signature)

设 $(V, \varphi)$ 为有限维实正交空间，设 $\varphi$ 在 $V$ 的一组基下的度量阵为对角阵 $G$，记 $G$ 的对角线上正、负、零分别共有 $r_+, r_-, r_0$ 个，称 $(r_+, r_-, r_0)$ 为 $(V, \varphi)$ 的一个符号。

- _注：严格来说我们这里还没有证明“符号”唯一。这个问题将在下面定理的充分性证明部分一并处理。_

#### 定理：实正交空间的等距分类

内容：有限维实正交空间等距同构当且仅当二者的（每个）符号（都）一致。

证明：

- 必要性：设 $(V_1, \varphi_1)$ 在基 $\alpha_1, \cdots, \alpha_n$ 下的度量阵和 $(V_2, \varphi_2)$ 在基 $\beta_1, \cdots, \beta_n$ 下的度量阵分别为对角阵：

$$
\begin{cases}
\text{diag}(a_1, \cdots, a_{r_+}, -b_1, \cdots, -b_{r_-}, 0, \cdots, 0), &\quad a_1, \cdots, a_{r_+}, b_1, \cdots, b_{r_-} > 0 \\
\text{diag}(c_1, \cdots, c_{r_+}, -d_1, \cdots, -d_{r_-}, 0, \cdots, 0), &\quad c_1, \cdots, c_{r_+}, d_1, \cdots, d_{r_-} > 0
\end{cases}
$$

- 构造映射 $\mathcal{A} \in \text{Hom}(V_1, V_2)$，使得：

$$
\begin{cases}
\mathcal{A} \alpha_i = \sqrt{\dfrac{a_i}{c_i}} \beta_i, &\quad \forall 1 \leq i \leq r_+ \\
\mathcal{A} \alpha_{j + r_+} = \sqrt{\dfrac{b_j}{d_j}} \beta_{j + r_+}, &\quad \forall 1 \leq j \leq r_- \\
\mathcal{A} \alpha_{k + r_0} = \beta_{k + r_0}, &\quad \forall 1 \leq k \leq r_0
\end{cases}
$$

- 容易验证 $\mathcal{A}$ 为从 $(V_1, \varphi_1)$ 到 $(V_2, \varphi_2)$ 的等距同构，故 $(V_1, \varphi_1), (V_2, \varphi_2)$ 等距同构。
- 充分性：设 $(V_1, \varphi_1)$ 有符号为 $(r_+, r_-, r_0)$，$(V_2, \varphi)$ 有符号为 $(r_+', r_-', r_0')$，$\mathcal{A}$ 为从 $(V_1, \varphi_1)$ 到 $(V_2, \varphi_2)$ 的等距同构。
- 由 $\mathcal{A}$ 为同构可知 $r_0 = n - \text{rank}(\varphi) = n - \text{rank}(\varphi') = r_0'$，故 $r_+ + r_- = r_+' + r_-'$。
- 取 $V$ 的这一组符号对应的正交直和分解：

$$
V = V_+ \oplus^{\perp} V_- \oplus^{\perp} \text{Rad}(V)
$$

- 其中 $V_+ = \displaystyle\left. \bigoplus_{\varphi(\alpha_i, \alpha_i) > 0} \right.^{\perp} \text{span}(\alpha_i), V_- = \left. \bigoplus_{\varphi(\alpha_i, \alpha_i) < 0} \right.^{\perp} \text{span}(\alpha_i)$，满足 $\dim V_+ = r_+, \dim V_- = r_-$。
- 对 $V'$ 同样操作。假设 $r_+ > r'_+$，构造线性映射 $\sigma = P_{V'_+} \circ \tau|_{V_+} \in \text{Hom}(V_+, V'_+)$，其中 $P$ 为投影映射。
- 由于 $\dim V_+ = r_+ > r'_+ = \dim V'_+$，$\sigma$ 不单，故 $\exists \alpha \in V_+ \backslash \{0\}, \text{s.t. } \sigma(\alpha) = 0$，故 $\tau(\alpha) \in V'_- \oplus \text{Rad}(V')$。
- 一方面，记 $\tau(\alpha) = \tau(\alpha)_- + \tau(\alpha)_0$，其中 $\tau(\alpha)_- \in V'_-, \tau(\alpha)_0 \in \text{Rad}(V')$，则有：

$$
\varphi'(\tau(\alpha), \tau(\alpha)) = \varphi'(\tau(\alpha)_- + \tau(\alpha)_0, \tau(\alpha)_- + \tau(\alpha)_0) = \varphi'(\tau(\alpha)_-, \tau(\alpha)_-) \leq 0
$$

- 但另一方面，由 $\tau$ 为等距同构及 $\alpha \in V_+ \backslash \{0\}$ 可知：

$$
\varphi'(\tau(\alpha), \tau(\alpha)) = \varphi(\alpha, \alpha) > 0
$$

- 矛盾！故 $r_+ \leq r'_+$。同理可证 $r_- \leq r'_-$。
- 相加即得 $r_+ + r_- \leq r_+' + r_-'$，故 $r_+ = r_+', r_- = r_-'$。
- 综上，$(r_+, r_-, r_0) = (r_+', r_-', r_0')$。

由有限维实正交空间与同维数实对称阵间的同构关系可知：

- 实对称阵合同当且仅当二者具有相同的秩和正、负惯性指数。

#### 定义：正交变换

设 $(V, \varphi)$ 为域 $F$ 上的正交空间，称其上的等距同构为正交变换，记其全体构成的集合为 $O(V, \varphi) \triangleq \text{Isom}(V, \varphi)$。

当 $V$ 有限维，设 $\dim V = n$，取 $V$ 的一组基 $\alpha_1, \cdots, \alpha_n$，设 $\varphi$ 在这组基下的度量阵为 $G$，$\tau \in O(V, \varphi)$ 在这组基下的矩阵为 $A$，则由等距同构可知：

$$
\forall x, y \in F^n, (Ax)^T G(Ay) = x^T Gy
$$

由 $x, y$ 的任意性即得：

$$
A^T GA = G
$$

反之，任一满足 $A^T G A = G$ 的可逆矩阵 $A$ 所确定的也是一个正交变换。

#### 定义：正交群

设 $G \in M_n(F)$ 为可逆对称阵，记：

$$
O(G) \triangleq \{A \in M_n(F) \mid A^T G A = G\}
$$

则易见 $O(G)$ 关于矩阵乘法成群，称为关于 $G$ 的正交群。

特别地，当 $F = \mathbb{R}$，$O(I_n)$ 即为 $n$ 阶实正交阵构成的群 $O(n)$。

#### 命题：正交变换的行列式

内容：当 $V$ 有限维，设 $\tau \in O(V, \varphi)$，则 $\det \tau = \pm 1$。

证明：

- 设 $\varphi, \tau$ 在 $V$ 的同一组基下的矩阵分别为 $G, A$，则 $A^T G A = G$，取行列式可知：

$$
(\det A)^2 \cdot \det G = \det G \Rightarrow \det A = \pm 1
$$

- 即 $\det \tau = \pm 1$。

#### 定义：旋转、反射

当 $V$ 有限维，设 $\tau \in O(V, \varphi)$：

- 称 $\tau$ 为旋转，若 $\det \tau = 1$。
- 称 $\tau$ 为反射，若 $\det \tau = -1$。

[例 6] $\tau_{\theta} \in O(\mathbb{R}^2, (\cdot, \cdot))$ 在标准正交基下的矩阵为 $\begin{bmatrix} \cos \theta & \sin \theta \\ -\sin \theta & \cos \theta \end{bmatrix}$，则 $\tau_{\theta}$ 为旋转。

------

下面来讨论一类特殊的反射。

#### 定义：Householder 反射

设 $(V, \varphi)$ 为正交空间，$\alpha \in V$ 非迷向，定义：

$$
\begin{aligned}
\sigma_{\alpha} : V &\to V \\
\beta &\mapsto \beta - 2 \frac{\varphi(\alpha, \beta)}{\varphi(\alpha, \alpha)} \alpha
\end{aligned}
$$

#### 命题：Householder 反射的性质

内容：设 $(V, \varphi)$ 为正交空间，$\alpha \in V$ 非迷向，则：

- (1) $\sigma_{\alpha}$ 为等距同构。
- (2) 将自身反向：$\sigma_{\alpha}(\alpha) = -\alpha$。
- (3) 在与自身垂直的超平面上保持不变：$\forall \beta \in \text{span}(\alpha)^{\perp}, \sigma_{\alpha}(\beta) = \beta$。
- (4) 当 $V$ 有限维，$\sigma_{\alpha}$ 为反射。
- _注：$\sigma_{\alpha}$ 的效果可以直观地理解为将向量关于超平面 $\text{span}(\alpha)^{\perp}$ 做”镜面反射“。_

证明：

- (1)(2)(3) 显然，下证 (4)。
- 取 $\text{span}(\alpha)^{\perp}$ 的一组基 $\beta_1, \cdots, \beta_{n - 1}$，则 $\alpha, \beta_1, \cdots, \beta_{n - 1}$ 为 $V$ 的一组基，且 $\sigma_{\alpha}$ 在这组基下的度量阵 $G$ 为：

$$
G = \begin{bmatrix}
-1 \\
& 1 \\
& & \ddots \\
& & & 1
\end{bmatrix}
$$

- 故有 $\det \sigma_{\alpha} = \det G = -1$，即 $\sigma_{\alpha}$ 为反射。

可以想见 Householder 反射是一种非常“基本”的反射了，因为它仅在一个一维子空间上有实际作用。下面讨论如何用这种基本的反射“张成”任意反射。

#### 引理：正交变换下正交补保持子空间的不变性

内容：设 $(V, \varphi)$ 为有限维正交空间，$\tau \in O(V, \varphi)$，$V$ 的子空间 $U$ 是 $\tau$-不变的，若 $\varphi|_U$ 非退化，则 $U^{\perp}$ 也是 $\tau$-不变的。

证明：

- (1) 由 $U$ 是 $\tau$-不变的可知 $\tau|_U \in \text{Hom}(U, U)$。
- 设 $\alpha \in U \backslash \{0\}$ 使得 $\tau(\alpha) = 0$，由 $\varphi|_U$ 非退化，可取 $\beta \in U$ 使得 $\varphi(\alpha, \beta) \neq 0$。
- 又由 $\tau$ 为等距同构可知：

$$
\varphi(\alpha, \beta) = \varphi(\tau(\alpha), \tau(\beta)) = \varphi(0, \tau(\beta)) = 0
$$

- 矛盾！故 $\ker \tau|_U$ 单，则 $\tau|_U$ 为同构。
- (2) $\forall \alpha \in U^{\perp}$，则：

$$
\forall \beta \in U, \varphi(\tau(\alpha), \beta) = \varphi(\alpha, \tau^{-1} \beta) = 0 \quad (\tau^{-1} \beta \in U)
$$

- 由定义可知 $\tau(\alpha) \in U^{\perp}$，故 $U^{\perp}$ 也是 $\tau$-不变的。

#### 引理：等长度向量的通过正交变换可达

内容：设 $(V, \varphi)$ 为域 $F$ 上的正交空间，$\text{char } F \neq 2$，$\alpha, \beta \in V$ 非迷向且 $\varphi(\alpha, \alpha) = \varphi(\beta, \beta) \neq 0$，则 $\exists \tau \in O(V, \varphi), \text{s.t. } \tau(\alpha) = \pm \beta$。

证明：

- 考虑 $\alpha - \beta$ 与 $\alpha + \beta$。
- (1) 若 $\alpha - \beta$ 非迷向，取 $\sigma_{\alpha - \beta}$：

$$
\begin{cases}
\sigma_{\alpha - \beta}(\alpha - \beta) = \beta - \alpha \\
\sigma_{\alpha - \beta}(\alpha + \beta) = \alpha + \beta
\end{cases} \Rightarrow \sigma_{\alpha - \beta}(\alpha) = \beta
$$

- (2) 若 $\alpha + \beta$ 非迷向，取 $\sigma_{\alpha + \beta}$ 可得 $\sigma_{\alpha - \beta}(\alpha) = -\beta$。
- (3) 若 $\alpha - \beta, \alpha + \beta$ 均迷向，则：

$$
\begin{cases}
\varphi(\alpha - \beta, \alpha - \beta) = 0 \\
\varphi(\alpha + \beta, \alpha + \beta) = 0
\end{cases} \Rightarrow \varphi(\alpha, \alpha) + \varphi(\beta, \beta) = 0
$$

- 再由 $\text{char } F \neq 2$ 和 $\varphi(\alpha, \alpha) = \varphi(\beta, \beta)$ 可知 $\varphi(\alpha, \alpha) = \varphi(\beta, \beta) = 0$，矛盾！

#### Cartan-Dieudonné (C-D) 定理

内容：设 $(V, \varphi)$ 为域 $F$ 上的 $n$ 维非退化正交空间，$\text{char } F \neq 2$，则任一 $\tau \in O(V, \varphi)$ 均可表示为至多 $n$ 个 Householder 反射的复合。

证明：

- 考虑对 $n$ 归纳：
- (1) 当 $n = 1$，$O(V, \varphi) = \{\pm \mathcal{I}\}$，规定 $\mathcal{I}$ 为 $0$ 个 Householder 反射的复合，而 $-\mathcal{I}$ 本身为 Householder 反射。
- (2) 当 $n > 1$，设命题对 $n - 1$ 成立，任取 $\alpha \in V \backslash \{0\}$：
- (I) 若 $\tau(\alpha) = \alpha$，则 $\tau|_{\text{span}(\alpha)^{\perp}}$ 为 $n - 1$ 维正交空间的正交变换，由 $\text{span}(\alpha)^{\perp}$ 非退化，据归纳假设有：

$$
\exists 0 \leq s \leq n - 1, \beta_1, \cdots, \beta_s \in V, \text{s.t. } \tau|_{\text{span}(\alpha)^{\perp}} = \sigma_{\beta_1}|_{\text{span}(\alpha)^{\perp}} \circ \cdots \circ \sigma_{\beta_s}|_{\text{span}(\alpha)^{\perp}}
$$

- 由正交变换下正交补保持子空间的不变性可知：

$$
\tau = \sigma_{\beta_1} \circ \cdots \circ \sigma_{\beta_s}
$$

- 据 $s \leq n - 1 < n$ 即证。
- (II) 若 $\tau(\alpha) \neq \alpha$，由 $\varphi(\alpha, \alpha) = \varphi(\tau(\alpha), \tau(\alpha))$ 及上述引理的构造可知：

$$
\exists \beta_0 \in V, \text{s.t. } \sigma_{\beta_0}(\tau(\alpha)) = \alpha
$$

- 考虑 $\sigma_{\beta_0} \circ \tau$ 即转化为 (I) 的情形，由 $\sigma_{\beta_0}^2 = \mathcal{I}$ 可知：

$$
\tau = \sigma_{\beta_0}^2 \circ \tau = \sigma_{\beta_0} \circ \sigma_{\beta_1} \circ \cdots \circ \sigma_{\beta_s}
$$

- 据 $s + 1 \leq n$ 即证。
- 综上 (1)(2)，$\tau$ 可分解为至多 $n$ 个 Householder 反射的复合。

### 欧氏空间

#### 定义：正定性

设 $(V, \varphi)$ 为实正交空间，称 $\varphi$ 正定，若：

$$
\begin{cases}
\forall \alpha \in V, \varphi(\alpha, \alpha) \geq 0 \\
\varphi(\alpha, \alpha) = 0 \Leftrightarrow \alpha = 0
\end{cases}
$$

#### 定义：欧氏空间 (Euclidean space)

称 $(V, \varphi)$ 为欧氏空间，若其为实正交空间且 $\varphi$ 正定，此时称 $\varphi$ 为内积。

方便起见，常记欧氏空间的内积 $\varphi(\alpha, \beta)$ 为 $(\alpha, \beta)$，与 $\mathbb{R}^n$ 上标准内积的符号一致。

#### 定义：标准正交基

称欧氏空间 $(V, \varphi)$ 的一组基 $e_1, \cdots, e_n$ 为标准正交基，若 $\varphi$ 在这组基下的度量阵为 $I_n$，即：

$$
\forall 1 \leq i, j \leq n, \varphi(e_i, e_j) = \delta_{i, j}
$$

#### 定理：Gram-Schmidt 正交化

内容：设 $(V, \varphi)$ 为 $n$ 维欧氏空间，则 $V$ 上存在标准正交基。

证明：

- 任取 $V$ 的一组基 $\alpha_1, \cdots, \alpha_n$，按下列方式依次构造 $V$ 的基 $e_1, \cdots, e_n$：

$$
\begin{aligned}
\beta_1 &= \alpha_1, & e_1 &= \frac{\beta_1}{||\beta_1||} \\
\beta_2 &= \alpha_2 - (\alpha_2, e_1) e_1, & e_2 &= \frac{\beta_2}{||\beta_2||} \\
& \cdots & & \cdots \\
\beta_k &= \alpha_k - \sum_{i = 1}^{k - 1} (\alpha_k, e_i) e_i, & e_k &= \frac{\beta_k}{||\beta_k||}
\end{aligned}
$$

- 则易见 $e_1, \cdots, e_n$ 为 $V$ 的一组标准正交基。

#### 命题：欧氏空间与 $\mathbb{R}^n$ 等距同构

内容：

- 设 $(V, \varphi)$ 为 $n$ 维欧氏空间，$e_1, \cdots, e_n$ 为 $V$ 的一组标准正交基，则映射

$$
\begin{aligned}
\sigma : V &\to \mathbb{R}^n \\
(e_1, \cdots, e_n) x &\mapsto x
\end{aligned}
$$

- 为从 $(V, \varphi)$ 到 $(\mathbb{R}^n, (\cdot, \cdot))$ 的等距同构。

进而任意 $n$ 维欧氏空间均与 $\mathbb{R}^n$ 等距同构。这意味着有限维欧氏空间的等距同构**由维数唯一决定**。


[例 7] 在 $M_n(\mathbb{R})$ 中定义 $\varphi(A, B) = \text{tr}(A^T B)$，则 $(M_n(\mathbb{R}), \varphi)$ 为欧氏空间。

证明：$\forall A = (a_{i, j})_{i, j} \in M_n(\mathbb{R}), \varphi(A, A) = \text{tr}(A^T A) = \displaystyle\sum_{i = 1}^n \sum_{j = 1}^n a_{i, j}^2 \geq 0$，且 $\varphi(A, A) = 0 \Leftrightarrow A = O$，故 $\varphi$ 正定，遂有 $(M_n(\mathbb{R}), \varphi)$ 为欧氏空间。

[例 8] 在 $\mathbb{R}[x]_n$ 中定义 $\varphi(f, g) = \displaystyle\sum_{k = 0}^{n + 1} f(k) g(k)$，则 $(\mathbb{R}[x]_n, \varphi)$ 为欧氏空间。

证明：$\forall f \in \mathbb{R}[x]_n, \varphi(f, f) = \displaystyle\sum_{k = 0}^n f(k)^2 \geq 0$，且 $\varphi(f, f) = 0$ 意味着 $f$ 在 $n + 2$ 个点 $0, \cdots, n + 1$ 上为 $0$，而 $\deg f \leq n$，则 $f = 0$，故 $\varphi$ 正定，遂有 $(\mathbb{R}[x]_n, \varphi)$ 为欧氏空间。

[例 9] 设 $V$ 为有限维实空间，$f, g$ 为 $V$ 上的内积，记 $\mathcal{A} = L_f^{-1} \circ L_g$，求证：

- (1) $\mathcal{A} \in \text{End}(V)$。
- (2) $\mathcal{A}$ 可对角化。
- (3) $\mathcal{A}$ 的特征值均为正数。

证明：

- (1) 线性映射 $L_f^{-1} \in \text{Hom}(V^*, V)$ 与 $L_g \in \text{Hom}(V, V^*)$ 的复合仍为线性映射，故 $\mathcal{A} \in \text{End}(V)$。
- (2) 由 $\mathcal{A} = L_f^{-1} \circ L_g$ 可知 $L_f \circ \mathcal{A} = L_g$，即：

$$
\forall \beta, \gamma \in V, f(\mathcal{A} \beta, \gamma) = g(\beta, \gamma) \quad (*)
$$

- 取 $(V, f)$ 的一组**标准正交基**，设 $\mathcal{A}$ 在这组基下的矩阵为 $A$，$g$ 在这组基下的度量阵为 $G$，将 $(*)$ 翻译为矩阵语言：

$$
\forall x, y \in \mathbb{R}^n, x^T Gy = (Ax)^T y = x^T A^T y
$$

- 由 $x, y$ 的任意性可知 $G = A^T$，则 $A = G^T = G$ 为实对称阵，故 $\mathcal{A}$ 可对角化。
- (3) 设 $\lambda$ 为 $\mathcal{A}$ 的特征值，$\alpha \neq 0$ 为 $\mathcal{A}$ 的属于特征值 $\lambda$ 的特征向量，即 $\mathcal{A} \alpha = \lambda \alpha$。
- 一方面，有 $f(\mathcal{A} \alpha, \alpha) = f(\lambda \alpha, \alpha) = \lambda f(\alpha, \alpha)$。
- 另一方面，由 $(*)$ 可知 $f(\mathcal{A} \alpha, \alpha) = g(\alpha, \alpha) > 0$。
- 又由于 $f(\alpha, \alpha) > 0$，可见 $\lambda = \dfrac{g(\alpha, \alpha)}{f(\alpha, \alpha)} > 0$，故 $\mathcal{A}$ 的特征值均为正数。

#### 定义：长度

设 $(V, \varphi)$ 为欧氏空间，$\forall \alpha \in V$，定义 $\alpha$ 的长度为：

$$
||\alpha|| \triangleq \sqrt{\varphi(\alpha, \alpha)}
$$

#### 命题：长度的基本性质

内容：设 $(V, \varphi)$ 为欧氏空间，则 $\forall \alpha, \beta \in V$，有：

- (1) Cauchy-Buniakowsky (C-B) 不等式：$\forall \alpha, \beta \in V, |(\alpha, \beta)| \leq ||\alpha|| \cdot ||\beta||$。
- (2) 勾股定理：$\alpha \perp \beta \Rightarrow ||\alpha + \beta||^2 = ||\alpha||^2 + ||\beta||^2$。
- (3) 三角不等式：$||\alpha + \beta|| \leq ||\alpha|| + ||\beta||$。

#### 定义：内积诱导的距离

设 $(V, \varphi)$ 为欧氏空间，$\forall \alpha, \beta \in V$，定义 $\alpha, \beta$ 间的距离为：

$$
d(\alpha, \beta) \triangleq ||\alpha - \beta||
$$

#### 命题：距离的基本性质

内容：设 $(V, \varphi)$ 为欧氏空间，则 $\forall \alpha, \beta \in V$，有：

- (1) $d(\alpha, \beta) \geq 0$，且 $d(\alpha, \beta) = 0 \Leftrightarrow \alpha = \beta$。
- (2) 三角不等式：$\forall \gamma \in V, d(\alpha, \beta) \leq d(\alpha, \gamma) + d(\gamma, \beta)$。

#### 定义：正交分解、正交投影

设 $(V, \varphi)$ 为有限维欧氏空间，$U$ 为 $V$ 的子空间，则 $\varphi$ 非退化，故有 $V = U \oplus^{\perp} U^{\perp}$。

由直和可知 $\forall \alpha \in V$，存在唯一分解 $\alpha = \beta + \gamma$，其中 $\beta \in U, \gamma \in U^{\perp}$，称为 $\alpha$ 的正交分解，并称

$$
P_U(\alpha) \triangleq \beta
$$

为 $\alpha$ 在 $U$ 上的正交投影。

#### 命题：补空间的在正交补上的投影总能映满正交补

内容：设 $(V, \varphi)$ 为有限维欧氏空间，$U$ 为 $V$ 的子空间，$W$ 为 $U$ 的补空间，则 $P_{U^{\perp}}(W) = U^{\perp}$。

证明：

- 考虑映射 $P_{U^{\perp}}|_W$，$\forall \alpha \in V, P_{U^{\perp}}(\alpha) = 0 \Leftrightarrow \alpha \in U$，则 $\ker P_{U^{\perp}}|_W = U \cap W = \{0\}$，故 $P_{U^{\perp}}|_W$ 单。
- 又由于 $\dim W = \dim V - \dim U = \dim U^{\perp}$，可见 $P_{U^{\perp}}|_W$ 为同构，则其满，故 $P_{U^{\perp}}(W) = U^{\perp}$。
