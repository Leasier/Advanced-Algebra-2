### 线性函数与对偶空间

#### 定义：线性函数、对偶空间

设 $V$ 为 $F$-空间，$V$ 上的线性函数指线性映射 $f : V \to F$，记其构成集合 $V^* \triangleq \text{Hom}_F(V, F)$，称为 $V$ 的对偶空间。

[例 1] 取 $V = M_{m \times n}(F)$，任取 $B \in V$，定义：

$$
\begin{aligned}
f_B : V &\to F \\
A &\mapsto \text{tr}(B^T A)
\end{aligned}
$$

由迹的性质可知 $f_B \in V^*$。

[例 2] 取 $V = F[x]_n$，任取 $t \in F$，定义：

$$
\begin{aligned}
\sigma_t : V &\to F \\
f &\mapsto f(t)
\end{aligned}
$$

由代入定理可知 $\sigma_t \in V^*$。

#### 命题：线性函数的核与像

内容：设 $V$ 为 $F$-空间，$f \in V^* \backslash \{0\}$，则 $\text{Im } f = F$，且 $\dim \ker f = n - 1$。

#### 定义：对偶基

设 $V$ 为 $n$ 维 $F$-空间，取 $V$ 的一组基 $\alpha_1, \cdots, \alpha_n$，$\forall 1 \leq i \leq n$，定义：

$$
\begin{aligned}
\alpha_i^* : V &\to F \\
\alpha_j &\mapsto \delta_{i, j}, &\quad \forall 1 \leq j \leq n
\end{aligned}
$$

即 $\forall \alpha = \displaystyle\sum_{j = 1}^n k_j \alpha_j \in V, \alpha_i^*(\alpha) = k_i$。

则 $\alpha_1^*, \cdots, \alpha_n^*$ 为 $V^*$ 的一组基，称为其对偶基，且有 $\forall f \in V^*, f = \displaystyle\sum_{i = 1}^n f(\alpha_i) \alpha_i^*$。

#### 命题：原空间与对偶空间的基变换

内容：设 $V$ 为 $n$ 维 $F$-空间，$\alpha_1, \cdots, \alpha_n$ 与 $\beta_1, \cdots, \beta_n$ 分别为 $V$ 的两组基，若 $\begin{bmatrix} \beta_1 & \cdots & \beta_n \end{bmatrix} = \begin{bmatrix} \alpha_1 & \cdots & \alpha_n \end{bmatrix} C$，则 $\begin{bmatrix} \beta_1^* & \cdots & \beta_n^* \end{bmatrix} = \begin{bmatrix} \alpha_1^* & \cdots & \alpha_n^* \end{bmatrix} C^{-T}$。

证明：

- 设 $C^{-1} = (\tilde{c}_{i, j})_{1 \leq i, j \leq n}$，则 $\beta_i^*(\alpha_j) = \displaystyle\sum_{k = 1}^n \tilde{c}_{k, j} \beta_i^*(\beta_k) = \tilde{c}_{i, j}$。
- 故有 $\beta_i^* = \displaystyle\sum_{j = 1}^n \tilde{c}_{i, j} \alpha_j^*$，即 $\begin{bmatrix} \beta_1^* & \cdots & \beta_n^* \end{bmatrix} = \begin{bmatrix} \alpha_1^* & \cdots & \alpha_n^* \end{bmatrix} C^{-T}$。

#### 定理：对偶（在同构意义下）的自反性

内容：

- 设 $V$ 为有限维 $F$-空间，$\forall \alpha \in V$，定义：

$$
\begin{aligned}
\tilde{\alpha} : V^* &\to F \\
f &\mapsto f(\alpha)
\end{aligned}
$$

- 在此基础上，定义：

$$
\begin{aligned}
\tau : V &\to V^{**} \\
\alpha &\mapsto \tilde{\alpha}
\end{aligned}
$$

- 则 $\tau$ 为同构。

证明：

- $\tau$ 的线性性显然，又由 $\dim V^{**} = \dim V^* = \dim V$ 可知只需证明 $\tau$ 单。
- 若 $\alpha \in V$ 使 $\tau(\alpha) = \tilde{\alpha} = 0$，则 $\forall f \in V^*, \tilde{\alpha}(f) = f(\alpha) = 0$。
- 当 $\alpha \neq 0$，设 $V = \text{span}(\alpha) \oplus U$，取 $f$ 使得 $\forall k \in F, \beta \in U, f(k \alpha + \beta) = k$，则 $f(\alpha) = 1$，矛盾！
- 故 $\alpha = 0$，即 $\ker \tau = \{0\}$，故 $\tau$ 单。

上述同构说明：$V$ 可视同 $V^*$ 上的线性函数，故向量空间可以视为线性函数的集合。

这意味着在某种程度上，$V, V^*$ 是“平等”的，并引出了下面“形式平等”的定义。

#### 定义：配对 (pair)

定义二元运算如下：

$$
\begin{aligned}
(\cdot, \cdot) : V \times V^* &\to F \\
(\alpha, f) &\mapsto f(\alpha) = \tilde{\alpha}(f) \triangleq \alpha(f)
\end{aligned}
$$

- 最后的记号 $\alpha(f)$ 想表达的正是“将线性函数应用于向量上”。
- 上面的 $V \times V^* \to F$ 经由同构 $V \cong \text{Hom}_F(F, V)$ 和 $F \cong \text{Hom}_F(F, F)$，（反过来写）也可以视为线性映射的合成 $\text{Hom}(V, F) \times \text{Hom}(F, V) \to \text{Hom}(F, F)$。

易见配对运算关于其两个参数均是线性的。

#### 定义：零化子 (annihilator)

$\forall S \subset V$，定义 $S$ 的零化子：

$$
\text{Ann}(S) = \{f \in V^* \mid \forall s \in S, f(s) = 0\} \subset V^*
$$

由定义易见 $\text{Ann}(S)$ 为 $V^*$ 的子空间。

[例 3] 令 $V = (F^n)^*$，则 $f \in V$ 可以表示为 $f(x) = \begin{bmatrix} a_1 & \cdots & a_n \end{bmatrix} x$，取 $S = \{f_1, \cdots, f_m\} \subset V$，则 $\text{Ann}(S)$ 为线性方程组 $\begin{cases} f_1(x) = 0 \\ \cdots \\ f_m(x) = 0 \end{cases}$ 的解空间。

下面详细讨论零化子是怎样在原空间与对偶空间的**子空间**之间建立关联的。

#### 命题：零化子的性质

内容：

- 设 $V$ 为 $F$-空间，则：
- (1) 维数：当 $V$ 有限维，设 $U$ 为 $V$ 的子空间，则 $\dim \text{Ann}(U) = \dim V - \dim U$。
- (2) 反序性：设 $U_1 \subset U_2 \subset V$，则 $\text{Ann}(U_2) \subset \text{Ann}(U_1)$。
- (3) 对合性：当 $V$ 有限维，设 $U$ 为 $V$ 的子空间，若将 $V$ 与 $V^{**}$（通过前述同构 $\tau$）等同，则 $\text{Ann}(\text{Ann}(U)) = U$。
- (4) 当 $V$ 有限维，设 $U_1, U_2$ 为 $V$ 的子空间，则 $\text{Ann}(U_1 + U_2) = \text{Ann}(U_1) \cap \text{Ann}(U_2), \text{Ann}(U_1 \cap U_2) = \text{Ann}(U_1) + \text{Ann}(U_2)$。

证明：

- (1) 取 $U$ 的一组基 $\alpha_1, \cdots, \alpha_r$，扩充为 $V$ 的一组基 $\alpha_1, \cdots, \alpha_r, \beta_1, \cdots, \beta_{n - r}$。
- $\forall f = \displaystyle\sum_{i = 1}^r \lambda_i \alpha_i^* + \sum_{i = 1}^{n - r} \mu_i \beta_i^* \in V^*$，由 $\forall 1 \leq j \leq r, f(\alpha_j) = 0$ 可知 $\forall 1 \leq j \leq r, \lambda_j = 0$，则 $f \in \text{span}(\beta_1^*, \cdots, \beta_{n - r}^*)$，故 $\text{Ann}(U) \subset \text{span}(\beta_1^*, \cdots, \beta_{n - r}^*)$。
- 同时，显见 $\text{span}(\beta_1^*, \cdots, \beta_{n - r}^*) \subset \text{Ann}(U)$，故 $\text{Ann}(U) = \text{span}(\beta_1^*, \cdots, \beta_{n - r}^*)$。
- 因此 $\dim \text{Ann}(U) = n - r = \dim V - \dim U$。
- (2) 显然。
- (3) 同 (1) 可知 $\text{Ann}(U) = \text{span}(\beta_1^*, \cdots, \beta_{n - r}^*), \text{Ann}(\text{Ann}(U)) = \text{span}(\alpha_1^{**}, \cdots, \alpha_r^{**})$。
- 因此 $\tau(U) = \tau(\text{span}(\alpha_1, \cdots, \alpha_r)) = \text{span}(\alpha_1^{**}, \cdots, \alpha_r^{**}) = \text{Ann}(\text{Ann}(U))$，即在将 $V$ 与 $V^{**}$ 等同的意义下，$\text{Ann}(\text{Ann}(U)) = U$ 成立。
- (4) 取 $U_1 \cap U_2$ 的一组基，将其分别扩充为 $U_1, U_2$ 的一组基，再将前面得到的 $U_1 + U_2$ 的一组基扩充为 $V$ 的一组基，同 (1) 即得，细节略去。

~~事实上，向量空间 $V$ 的子空间集合 $\text{Sub}(V)$ 关于 $+, \cap$ 构成格。当 $V$ 有限维，(4) 指出 $\text{Ann}$ 为 $\text{Sub}(V) \to \text{Sub}(V^*)$ 的**格反同态**（即和与交运算取反），(3) 则进一步指出 $\text{Ann}$ 为**格反同构**。~~

#### 定义：对偶映射

设 $\mathcal{A} \in \text{Hom}(V_1, V_2)$，定义：

$$
\begin{aligned}
\mathcal{A}^* : V_2^* &\to V_1^* \\
f &\mapsto f \circ \mathcal{A}
\end{aligned}
$$

称为 $\mathcal{A}$ 的对偶映射。

由定义立即可得 $\mathcal{A}^* \in \text{Hom}(V_2^*, V_1^*)$，且有如下性质：

$$
\forall \alpha \in V_1, \beta \in V_1^*, (\alpha, \mathcal{A}^* \beta) = (\mathcal{A} \alpha, \beta)
$$

#### 命题：对偶映射的核与像的性质

内容：

- 设 $V_1, V_2$ 为有限维 $F$-空间，$\mathcal{A} \in \text{Hom}(V_1, V_2)$，则：
- (1) $\ker \mathcal{A}^* = \text{Ann}(\text{Im } \mathcal{A})$。
- (2) 若将 $V_1$ 与 $V_1^{**}$ 等同，$\text{Ann}(\text{Im } \mathcal{A}^*) = \ker \mathcal{A}$。
- (3) $\text{Im } \mathcal{A}^* = \text{Ann}(\ker \mathcal{A})$。
- (4) 若将 $V_2$ 与 $V_2^{**}$ 等同，$\text{Ann}(\ker \mathcal{A}^*) = \text{Im } \mathcal{A}$。

证明：

- (1)

$$
\begin{array}{l}
& f \in \ker \mathcal{A}^* \\
\Leftrightarrow & \mathcal{A}^* f = 0 \\
\Leftrightarrow & \forall \alpha \in V_1, (f \circ \mathcal{A}) \alpha = 0 \\
\Leftrightarrow & \forall \alpha \in V_1, f(\mathcal{A} \alpha) = 0 \\
\Leftrightarrow & \forall \beta \in \text{Im } \mathcal{A}, f(\beta) = 0 \\
\Leftrightarrow & f \in \text{Ann}(\text{Im } \mathcal{A})
\end{array}
$$

- 故有 $\ker \mathcal{A}^* = \text{Ann}(\text{Im } \mathcal{A})$。
- (2)

$$
\begin{array}{l}
& \alpha \in \ker \mathcal{A} \\
\Leftrightarrow & \mathcal{A} \alpha = 0 \\
\Leftrightarrow & \forall f \in V_2^*, f(\mathcal{A} \alpha) = 0 \\
\Leftrightarrow & \forall f \in V_2^*, (f \circ \mathcal{A}) \alpha = 0 \\
\Leftrightarrow & \forall f \in V_2^*, (\mathcal{A}^* f) \alpha = 0 \\
\Leftrightarrow & \forall g \in \text{Im } \mathcal{A}^*, g(\alpha) = 0 \\
\Leftrightarrow & \forall g \in \text{Im } \mathcal{A}^*, \tilde{\alpha}(g) = 0 \\
\Leftrightarrow & \tilde{\alpha} \in \text{Ann}(\text{Im } \mathcal{A}^*)
\end{array}
$$

- 故有 $\tau(\ker \mathcal{A}) = \text{Ann}(\text{Im } \mathcal{A}^*)$，将 $V_1$ 与 $V_1^{**}$ 等同后即得 $\text{Ann}(\text{Im } \mathcal{A}^*) = \ker \mathcal{A}$。
- (3) 一方面，$\forall \mathcal{A}^* \alpha^* \in \text{Im } \mathcal{A}^*$，$\forall \beta \in \ker \mathcal{A}, (\mathcal{A}^* \alpha^*) \beta = \alpha^*(\mathcal{A} \beta) = 0$，故 $\text{Im } \mathcal{A}^* \subset \text{Ann}(\ker \mathcal{A})$。
- 另一方面，$\forall f \in \text{Ann}(\ker \mathcal{A})$，设 $V_2 = \text{Im } \mathcal{A} \oplus U$，定义：

$$
\begin{aligned}
g : V_2 &\to F \\
\mathcal{A} \alpha + \beta &\mapsto \alpha, &\quad \forall \alpha \in V_1, \beta \in U
\end{aligned}
$$

- $g$ 的线性性与良定义性显然，同时由定义可知 $f = \mathcal{A}^* g$，故 $\text{Ann}(\ker \mathcal{A}) \subset \text{Im } \mathcal{A}^*$。
- 综上，$\text{Im } \mathcal{A}^* = \text{Ann}(\ker \mathcal{A})$。
- (4) 类似 (2) 做即可，略去。

#### 命题：矩阵视角下的对偶映射

内容：设 $V_1, V_2$ 分别为 $m, n$ 维 $F$-空间，$\mathcal{A} \in \text{Hom}(V_1, V_2)$，分别取 $V_1, V_2$ 的两组基 $\alpha_1, \cdots, \alpha_m$ 和 $\beta_1, \cdots, \beta_n$，设 $\mathcal{A} \begin{bmatrix} \alpha_1 & \cdots & \alpha_m \end{bmatrix} = \begin{bmatrix} \beta_1 & \cdots & \beta_n \end{bmatrix} A$，则 $\mathcal{A}^* \begin{bmatrix} \beta_1^* & \cdots & \beta_n^* \end{bmatrix} = \begin{bmatrix} \alpha_1^* & \cdots & \alpha_m^* \end{bmatrix} A^T$。

证明：设出 $A$ 的分量，按定义展开 $(\mathcal{A}^* \beta_i^*) \alpha_j$ 即可，略去。

### 双线性函数

#### 定义：双线性函数

设 $V_1, V_2$ 为 $F$-空间，$\varphi : V_1 \times V_2 \to F$，称 $\varphi$ 为 $V_1, V_2$ 上的双线性函数，若其关于每一个参数都是线性的。

记 $\text{Bil}_F(V_1, V_2)$ 为所有 $V_1, V_2$ 上的双线性函数构成的集合，易见其关于逐点加法和数乘构成 $F$-空间。

同样，在不致歧义的前提下，$\text{Bil}_F(V_1, V_2)$ 常略作 $\text{Bil}(V_1, V_2)$。

[例 4] 设 $f \in V_1^*, g \in V_2^*$，定义映射 $\varphi_{f, g}(\alpha, \beta) = f(\alpha) g(\beta)$，不难验证 $\varphi_{f, g}$ 也是一个双线性函数。

#### 双线性函数的矩阵表示

设 $V_1, V_2$ 有限维，分别取二者的两组基 $\alpha_1, \cdots, \alpha_n$ 和 $\beta_1, \cdots, \beta_m$。

设 $\alpha = \begin{bmatrix} \alpha_1 & \cdots & \alpha_n \end{bmatrix} x \in V_1, \beta = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} y \in V_2$，则：

$$
\varphi(\alpha, \beta) = \sum_{i = 1}^n \sum_{j = 1}^m x_i \cdot \varphi(\alpha_i, \beta_j) \cdot y_j
$$

记 $A = (\varphi(\alpha_i, \beta_j))_{i, j} \in M_{n \times m}(F)$，则有矩阵形式：

$$
\varphi(\alpha, \beta) = x^T Ay
$$

称 $A$ 为 $\varphi$ 在该基下的度量阵。易见选定基时，$\varphi$ 与 $A$ 一一对应，故有推论：

$$
\text{Bil}_F(V_1, V_2) \cong M_{n \times m}(F)
$$

同时易见 $\varphi(\alpha_i, \beta_j) = \varphi_{\alpha_i^*, \beta_j^*}(\alpha, \beta)$，故 $\{\varphi_{\alpha_i^*, \beta_j^*}\}$ 即为 $\text{Bil}(V_1, V_2)$ 的一组基。

#### 基变换下的双线性函数

设有基变换 $\begin{bmatrix} \alpha'_1 & \cdots & \alpha'_n \end{bmatrix} = \begin{bmatrix} \alpha_1 & \cdots & \alpha_n \end{bmatrix} P, \begin{bmatrix} \beta'_1 & \cdots & \beta'_m \end{bmatrix} = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} Q$，$\alpha, \beta$ 在新基下的坐标为 $x', y'$，则：

$$
\varphi(\alpha, \beta) = x^T Ay = (Px')^T A (Qy') = (x')^T (P^T AQ) y'
$$

因此 $\varphi$ 在新基下的度量阵为 $A' = P^T AQ$。

特别地，当 $V_1 = V_2 = V$ 且二者取同一组基，则 $A' = P^T AP$，即度量阵在基变换下合同。

#### 定义：正交子空间

设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，$U_1, U_2$ 分别为 $V_1, V_2$ 的子空间，定义 $U_1, U_2$ 关于 $\varphi$ 的正交子空间分别为：

$$
\begin{cases}
U_1^{\perp} = \{\beta \in V_2 \mid \forall \alpha \in U_1, \varphi(\alpha, \beta) = 0 \} \\
U_2^{\perp} = \{\alpha \in V_1 \mid \forall \beta \in U_2, \varphi(\alpha, \beta) = 0 \}
\end{cases}
$$

易见 $U_1^{\perp}, U_2^{\perp}$ 分别为 $V_2, V_1$ 的子空间。

特别地，当 $V_1 = V_2 = V$，记：

$$
\begin{cases}
U^{\perp_R} = \{\beta \in V \mid \forall \alpha \in U, \varphi(\alpha, \beta) = 0 \} \\
U^{\perp_L} = \{\alpha \in V \mid \forall \beta \in U, \varphi(\alpha, \beta) = 0 \}
\end{cases}
$$

其中 $V^{\perp_R}, V^{\perp_L}$ 分别称为 $V$ 的右根和左根。

#### 正交子空间的矩阵表示

设 $V_1, V_2$ 有限维，利用双线性函数的矩阵表示 $\varphi(\alpha, \beta) = x^T Ay$：

$$
\begin{aligned}
V_1^{\perp} &= \{\beta = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} y \in V_2 \mid \forall x \in F^n, x^T Ay = 0 \} \\
&= \{\beta = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} y \in V_2 \mid Ay = 0 \} \\
&= \{\beta = \begin{bmatrix} \beta_1 & \cdots & \beta_m \end{bmatrix} y \in V_2 \mid y \in \ker A \} \\
\end{aligned}
$$

同理有：

$$
V_2^{\perp} = \{\alpha = \begin{bmatrix} \alpha_1 & \cdots & \alpha_n \end{bmatrix} x \in V_1 \mid x \in \ker A^T \}
$$

可见 $\dim V_1^{\perp} = m - \text{rank}(A), \dim V_2^{\perp} = n - \text{rank}(A)$。

#### 定义：退化

设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$：

- 称 $\varphi$ 左退化，若 $V_2^{\perp} \neq \{0\}$；反正称其非左退化。
- 称 $\varphi$ 右退化，若 $V_1^{\perp} \neq \{0\}$；反正称其非右退化。
- 称 $\varphi$ 非退化，若其非左退化且非右退化。

#### 命题：退化的充要条件

内容：

- 设 $V_1, V_2$ 为有限维 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，则：
- (1) $\varphi$ 左退化当且仅当 $\varphi$ 在某组基下的度量阵列不满秩。
- (2) $\varphi$ 右退化当且仅当 $\varphi$ 在某组基下的度量阵行不满秩。
- (3) $\varphi$ 非退化当且仅当 $\dim V_1 = \dim V_2$ 且 $\varphi$ 在某组基下的度量阵可逆。

#### 定义：左、右伴随

设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，则 $\varphi$ 诱导出下面两个线性映射：

- (1) 左伴随

$$
\begin{aligned}
L_{\varphi} : V_1 &\to V_2^* \\
\alpha &\mapsto L_{\varphi}(\alpha) : \beta \mapsto \varphi(\alpha, \beta)
\end{aligned}
$$

- (2) 右伴随

$$
\begin{aligned}
R_{\varphi} : V_2 &\to V_1^* \\
\beta &\mapsto R_{\varphi}(\beta) : \alpha \mapsto \varphi(\alpha, \beta)
\end{aligned}
$$

~~你就说这是不是一种 partial application 吧！~~

#### 命题：左、右伴随的核与像

内容：

- 设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，则：
- (1) $\ker L_{\varphi} = V_2^{\perp}, \ker R_{\varphi} = V_1^{\perp}$。
- (2) 当 $V_1, V_2$ 有限维，设 $\varphi$ 在某组基下的度量阵为 $A$，则 $\text{rank}(L_{\varphi}) = \text{rank}(R_{\varphi}) = \text{rank}(A)$。

证明：

- (1) 由定义立即可得。
- (2) 设 $\varphi$ 在 $V_1$ 的基 $\alpha_1, \cdots, \alpha_n$ 和 $V_2$ 的基 $\beta_1, \cdots, \beta_m$ 下的度量阵为 $A = (a_{i, j})_{i, j}$。
- 考察 $L_{\varphi}$ 在 $V_1$ 的基 $\alpha_1, \cdots, \alpha_n$ 和 $V_2^*$ 的基 $\beta_1^*, \cdots, \beta_m^*$ 下的表现：

$$
(L_{\varphi}(\alpha_i))(\beta_j) = \varphi(\alpha_i, \beta_j) = a_{i, j}
$$

- 则 $L_{\varphi}$ 在这组基下的矩阵为 $A$，故 $\text{rank}(L_{\varphi}) = \text{rank}(A)$。
- 同理可知 $R_{\varphi}$ 在 $V_2$ 的基 $\beta_1, \cdots, \beta_m$ 和 $V_1^*$ 的基 $\alpha_1^*, \cdots, \alpha_n^*$ 下的矩阵为 $A^T$，故 $\text{rank}(L_{\varphi}) = \text{rank}(A^T) = \text{rank}(A)$。

#### 推论：退化与左、右伴随的关系

内容：

- 设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$，则：
- (1) $\varphi$ 非左退化 $\Leftrightarrow L_{\varphi}$ 单，$\varphi$ 非右退化 $\Leftrightarrow R_{\varphi}$ 单。
- (2) 当 $V_1, V_2$ 有限维，$\varphi$ 非退化 $\Leftrightarrow L_{\varphi}$ 为同构 $\Leftrightarrow R_{\varphi}$ 为同构。
- _此时可以视 $V_1, V_2$ 为彼此的对偶空间。_

注意到 (2) 要求**有限维**，事实上无限维的情况可以构造反例如下：

- 令 $V_1 = V_2 = \mathbb{R}[x]$，定义 $\varphi \in \text{Bil}(\mathbb{R}[x], \mathbb{R}[x])$ 满足 $\varphi(x^i, x^j) = \delta_{i, j}$，则：

$$
\forall f(x) = \sum_{i = 0}^n a_i x^i, g(x) = \sum_{i = 0}^n b_i x^i \in \mathbb{R}[x], \varphi(f, g) = \sum_{i = 0}^n a_i b_i
$$

- (I) 若 $f(x) = \displaystyle\sum_{i = 0}^n a_i x^i \in \mathbb{R}[x]$ 使得 $L_{\varphi}(f) = 0$，则 $(L_{\varphi}(f))(f) = \varphi(f, f) = \displaystyle\sum_{i = 0}^n a_i^2 = 0$，故 $a_0 = \cdots = a_n = 0$，即 $f = 0$。
- 故 $L_{\varphi}$ 单，则 $\varphi$ 非左退化。同理可知 $\varphi$ 非右退化，故 $\varphi$ 非退化。
- (II) 定义 $T \in \mathbb{R}[x]^*$ 满足 $T(x^i) = 1$，则：

$$
\forall f(x) = \sum_{i = 0}^n a_i x^i \in \mathbb{R}[x], T(f) = \sum_{i = 0}^n a_i
$$

- 若 $\tilde{T} \in \mathbb{R}[x]$ 使得 $L_{\varphi}(\tilde{T}) = T$，则 $\forall i \in \mathbb{N}$，应用 $x^i$ 可见 $\tilde{T}$ 的 $x^i$ 项为 $1$，与 $\tilde{T}$ 系数组只有有限个 $0$ 矛盾。
- 故 $L_{\varphi}$ 不为同构。同理可知 $R_{\varphi}$ 也不为同构。

#### Riesz 表示定理（有限维 ver.）

内容：设 $V_1, V_2$ 为 $F$-空间，$\varphi \in \text{Bil}(V_1, V_2)$ 非退化，则：

$$
\begin{cases}
\forall f \in V_1^*, \exists ! \beta \in V_2, \text{s.t. } f = R_{\varphi}(\beta) \\
\forall g \in V_2^*, \exists ! \alpha \in V_1, \text{s.t. } g = L_{\varphi}(\alpha)
\end{cases}
$$

证明：由上述推论 (2) 立即可得。

#### 定义 & 定理：伴随映射

设 $V_1, V_2$ 为 $F$-空间，$\varphi_1 \in \text{Bil}(V_1, V_1), \varphi_2 \in \text{Bil}(V_2, V_2)$ 非退化，给定 $\mathcal{A} \in \text{Hom}(V_1, V_2)$，称 $\mathcal{A}^* \in \text{Hom}(V_2, V_1)$ 为其伴随映射，若：

$$
\forall \alpha \in V_1, \beta \in V_2, \varphi_1(\alpha, \mathcal{A}^* \beta) = \varphi_2(\mathcal{A} \alpha, \beta) \quad (*)
$$

当 $V_1, V_2$ **有限维**，$\forall \beta \in V_2$，定义线性映射：

$$
\begin{aligned}
f_{\beta} : V_1 &\to F \\
\alpha &\mapsto \varphi_2(\mathcal{A} \alpha, \beta)
\end{aligned}
$$

由 $\varphi_1$ 非退化且 $f_{\beta} \in V_1^*$，据 Riesz 表示定理可知：

$$
\exists ! \alpha_0 \in V_1, \text{s.t. } f_{\beta} = L_{\varphi_1}(\alpha_0)
$$

令 $\mathcal{A}^* \beta = \alpha_0$，则满足 $(*)$ 的 $\mathcal{A}^* : V_2 \to V_1$ **存在且唯一**。下面验证其保线性：

- (1) 保持加法：$\forall \beta_1, \beta_2 \in V_2$，$\forall \alpha \in V_1$，有：

$$
\begin{array}{l}
& \varphi_1(\alpha, \mathcal{A}^*(\beta_1 + \beta_2)) \\
= & \varphi_2(\mathcal{A}(\alpha), \beta_1 + \beta_2) \\
= & \varphi_2(\mathcal{A}(\alpha), \beta_1) + \varphi_2(\mathcal{A}(\alpha), \beta_2) \\
= & \varphi_1(\alpha, \mathcal{A}^*(\beta_1)) + \varphi_1(\alpha, \mathcal{A}^*(\beta_2)) \\
= & \varphi_1(\alpha, \mathcal{A}^*(\beta_1) + \mathcal{A}^*(\beta_2))
\end{array}
$$

- 故 $R_{\varphi_1}(\mathcal{A}^*(\beta_1 + \beta_2)) = R_{\varphi_1}(\mathcal{A}^*(\beta_1) + \mathcal{A}^*(\beta_2))$，由 $\varphi_1$ 非退化可知 $R_{\varphi_1}$ 单，遂有：

$$
\mathcal{A}^*(\beta_1 + \beta_2) = \mathcal{A}^*(\beta_1) + \mathcal{A}^*(\beta_2)
$$

- (2) 保持数乘：同 (1) 可证，略去。

综上，$\mathcal{A}$ 的伴随映射 $\mathcal{A}^*$ **存在且唯一**。

------

上面讨论的 $\mathcal{A}$ 作用于第一个参数的伴随映射称为左伴随，记作 $\mathcal{A}^{L*}$。同理还可以定义 $\mathcal{A}$ 作用于第二个参数的右伴随 $\mathcal{A}^{R*} \in \text{Hom}(V_2, V_1)$：

$$
\forall \alpha \in V_1, \beta \in V_2, \varphi_1(\mathcal{A}^{R*} \beta, \alpha) = \varphi_2(\beta, \mathcal{A} \alpha)
$$

### 度量空间

#### 定义：度量空间

设 $V$ 为 $F$-空间，$\varphi \in \text{Bil}(V, V)$，则称二元组 $(V, \varphi)$ 为度量空间，其中 $\varphi$ 称为 $V$ 的度量。

特别地，当 $\varphi$ 非退化，称 $(V, \varphi)$ 为非退化 / 正则度量空间。
